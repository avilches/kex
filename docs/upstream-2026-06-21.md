# Upstream sync plan -- 2026-06-21

## Context

Fork: terax-ai / Kex (no AI subsystem, enhanced Workspace/Pane/Panel model)
Upstream: https://github.com/crynta/terax-ai.git
Fork philosophy: no AI runtime, no API keys, no keychain; focus on a clean, fast, terminal-first workspace; ultra-light bundle (~7-8 MB).

Last synced upstream commit (LAST_SYNC): `8e1c4743fb4efcf1f3d089457c32dc1326552683`
Upstream HEAD at plan creation (UPSTREAM_HEAD): `b656fe3816d0ddaefb17e7acff5e16889f6e939a`
Commits to process: 34 real commits, 0 merges (`8e1c474..b656fe3`).

Note on git history: the user asked specifically whether the upstream is working on the git history module. It is not. No commit in this range touches `src/modules/git-history/` or the `git_log` / `git_show_commit` Rust commands. The only git-related change is `0eac4a6` on `git/utils.rs` (staging fix), covered in Bucket B below.

Every `Approach` and effort note below is provisional and was written from the upstream's perspective. Re-verify it against the fork's actual current code at execution time before applying. Use the `superpowers:executing-plans` skill to run this plan with review checkpoints.

## Execution log

Branch: `sync/upstream-2026-06-21` (worktree `.claude/worktrees/sync-upstream-2026-06-21`).

Bug fixes (Bucket B) executed 2026-06-21:

- `0eac4a6` git stage when parent dir deleted: APLICADO (cherry-pick, clean; brings its own unit tests).
- `7baee7c` explorer close menu after delete: DESCARTADO (no aplica; fork uses a dedicated `DeleteEntryModal`, the context menu already closes).
- `7f972aa` + `a1fca84` terminal pre-attach input buffer: APLICADO (manual merge onto the fork's string-keyed session model; overflow rule extracted as a pure function with a new test `pendingInput.test.ts`).
- `1691e73` pty reap on child exit: APLICADO (manual merge; kex-named threads, ConPTY lifecycle lock, window_sessions preserved).
- `2711841` fish cwd with starship: APLICADO (manual merge onto `__kex_` names; reinstall prompt for every fish session, not just block mode).
- `6019e04` monospace fallback for custom fonts: DESCARTADO (already solved better by the fork's `4803abc`: platform default stacks, `normalizeFontFamilies`, commit-on-blur `FontFamilyInput`, plus editor font support).
- `cbe1ad4` drop tokenlens + woff duplicates: APLICADO (partial; woff2-only @font-face for JetBrains Mono, removed dead `tokenlens` dep; AI-elements hunks absent in the fork).

Quality suite after the bug-fix chunk: pnpm lint exit 0 (135 warnings preexisting), pnpm check-types OK, pnpm test 425 passed, cargo clippy clean, cargo test --locked 260 passed.

Merged to `main` 2026-06-21 (fast-forward, HEAD `161ed21`). The five applied fixes are now on `main`; the two discarded ones need no code. `sync/upstream-2026-06-21` and its worktree still exist at the same HEAD and can be reused or removed.

Bucket C (features) and the dependabot bumps remain pending user decisions; not started in this chunk.

## Next session — how to resume

State at last stop (2026-06-21):

- Bug fixes (Bucket B): DONE, merged to `main` (`161ed21`).
- LAST_SYNC stays `8e1c474` until the whole sync is closed. Only bump the FORK.md sync-log "last synced" pointer to UPSTREAM_HEAD `b656fe3` once every Bucket C item below is APLICADO or DESCARTADO. Do not advance it while items are still EVALUAR, or their commits would be skipped on the next sync.
- Decide where to work: a fresh worktree under `.claude/worktrees/` is cleanest for the feature merges (they touch shared files). The existing `sync/upstream-2026-06-21` branch is already == main and can be reused.

Remaining work, each needs a user decision before applying (re-verify the approach against the live code first — the fork may already cover it, as happened with two of the bug fixes):

1. Editor syntax highlighting (`b7eb8d6` + `90aeab5` + `b656fe3`): cherry-pick chain, likely low risk. Confirm no heavy CodeMirror language deps land in the bundle (eager-budget test).
2. OSC 52 clipboard handler (`0c647c4`): SECURITY decision needed — gate behind a default-off setting, or accept upstream always-on? Confirm write-only.
3. Confirm-quit with a running terminal process (`d782f7d`): reconcile with the fork's existing `closeQueue.ts` + `useTabCloseGuards.ts`.
4. Editor theme picker + new themes (`35f8711` + `1afe8d5`): largest item; skip the `AiDiffPane.tsx` hunk (absent here); confirm `cmThemes.ts` pulls no heavy dep.
5. WSL env restore on reopen (`4a6d803`): spaces-coupled; extract only the `workspace.rs` Rust part if separable, else skip and document.
6. Dependabot bumps (`6dc2648`, `53c3a60`, `5a656c3`, `deded91`, `1142fba`): re-derive natively (`pnpm update` / `cargo update -p`), not cherry-pick; skip AI-only packages.

The per-item detail, divergence marks, and approaches are in "New features to evaluate (Bucket C)" below. Update each item's Decision state in place when resolved.

## Removed surface (skip all changes to these paths)

From `docs/FORK.md` "What has been removed":

- AI subsystem (frontend): `src/modules/ai/`, `src/components/ai-elements/`, `AiInputBar` / `WorkspaceInputBar` AI composer, AI/Agents Settings sections (`ModelsSection.tsx` AI surface), AI controls in StatusBar, `AiDiffPane.tsx`, AI wiring in `App.tsx`.
- AI subsystem (Rust): `src-tauri/src/modules/net.rs`, `src-tauri/src/modules/secrets.rs`.
- npm AI deps: `ai`, `@ai-sdk/*`, `streamdown`.
- `src/modules/spaces/` (Spaces, tab grouping): rejected, conflicts with the Workspace model.
- `src/modules/tabs/` (flat tab model + tab DnD): not present in the fork.
- `src/modules/dnd/` (upstream dnd foundation): superseded by `WorkspaceDndProvider`.
- Nix packaging (`flake.nix`, `nix/`, `update-nix-sources.yml`).

## Ignored changes (Bucket A -- removed surface)

- AI: `9162109` (Grok 4.3 / Opus 4.8 / GPT-5.5 models), `f24c90e` (Groq + Whisper.cpp STT providers), `ed6900f` (macOS microphone entitlement to audio-input, exists only for voice/STT). Touches `src/modules/ai/`, `ModelsSection.tsx`, `entitlements.plist`, `src/modules/settings/store.ts` AI keys. Ignored.
- Spaces / Tabs: `5b8b86c` (refactor spaces pointer DnD), `c3e7269` (Ctrl+Tab MRU switcher + space-delete PTY leak), `9f256cc` (drag-to-reorder tabs), `10af844` (commit switcher on modifier release), `ed6a1bf` (select on pointer release), `61b526a` (skip close dialog for last tab in space). Modules `spaces/` and `tabs/` absent. Ignored. New files `TabSwitcherHud.tsx`, `useTabSwitcher.ts`, `planSpaceRemoval.test.ts`, `reorderTabsByGap.test.ts`, `activeSpace.ts`, `SpaceSwitcher.tsx` belong here. Ignored.
- Upstream infra: `4e6a478` + `da8ba3e` (CodeRabbit config `.coderabbit.yaml`), `4b95be5` (version bump v0.8.1), `f3136ff` (README badges + block-terminal feature blurb), Nix sources workflow. Ignored.

## Changes to apply (Bucket B -- shared code)

### Git stage fix when parent directory was deleted

- Files: `src-tauri/src/modules/git/utils.rs`
- Upstream commits: `0eac4a6`
- Divergence: fork CLEAN since LAST_SYNC; inverse diff matches the commit exactly. Cherry-pick viable.
- Approach: `git cherry-pick 0eac4a6`
- Rationale: correctness fix in the Source Control surface (gated git command). High value, low risk. A core subsystem change (git) needs the invariant locked by a test if cherry-pick does not bring one.

### Explorer: close context menu after deleting an item

- Files: `src/modules/explorer/FileExplorer.tsx`
- Upstream commits: `7baee7c`
- Divergence: fork DIVERGED (48 commits on this file). REQUIRES MANUAL MERGE, do not cherry-pick.
- Status: DESCARTADO (2026-06-21, no aplica). The upstream bug is specific to its inline "click again to confirm" pattern, which calls `e.preventDefault()` on every click and so leaves the Radix context menu open after the actual delete. The fork already diverged from that design: the Delete entry (`TreeRow.tsx:460`) calls `actions.requestDelete(path, isDir)` with no `preventDefault`, opening a dedicated `DeleteEntryModal`, so the context menu closes normally. No code change needed.
- Rationale: small UX correctness fix, already satisfied by the fork's modal-based delete.

### Terminal: buffer input typed before the PTY attaches, bounded

- Files: `src/modules/terminal/lib/useTerminalSession.ts` (DIVERGED 23), `src/modules/terminal/lib/osc-handlers.ts` (DIVERGED 2)
- Upstream commits: `7f972aa` (buffer input before pty attaches), `a1fca84` (bound the pre-attach input buffer)
- Divergence: REQUIRES MANUAL MERGE.
- Instructions: read both `git show` diffs. Port the pre-attach input queue plus its size cap into the fork's `useTerminalSession`. Verify against the fork's pty-bridge attach lifecycle (panels are never unmounted here, so confirm the attach path still matches).
- Rationale: avoids dropped keystrokes during spawn; the cap prevents unbounded growth. Apply both together; the second commit fixes the first.

### PTY: reap session on child exit to free stranded pseudoconsole

- Files: `src-tauri/src/modules/pty/session.rs`
- Upstream commits: `1691e73`
- Divergence: DIVERGED (10). REQUIRES MANUAL MERGE.
- Instructions: read `git show 1691e73`. Port the child-exit reaping into the fork's `session.rs`, respecting the fork's `SPAWN_LOCK` mutex and per-session Job Object (Windows). Do not regress the ConPTY first-tab stability guard.
- Rationale: resource leak fix (stranded pseudoconsole). Core subsystem: add or keep a test that locks the reap-on-exit invariant.

### PTY: preserve fish cwd tracking with starship

- Files: `src-tauri/src/modules/pty/scripts/init.fish`
- Upstream commits: `2711841`
- Divergence: DIVERGED (4). REQUIRES MANUAL MERGE (the fork already integrated blocks into init.fish).
- Instructions: read `git show 2711841`. Apply the OSC 7 cwd emission fix for the starship-prompt case to the fork's `init.fish` without disturbing the existing blocks/OSC 133 integration.
- Rationale: correctness for fish + starship users.

### Terminal: monospace fallback for custom fonts and family parsing

- Files: `src/lib/fonts.ts` (DIVERGED 4), `src/styles/fonts.css` (DIVERGED 2), possibly `src/styles/tokens.ts`
- Upstream commits: `6019e04`
- Status: DESCARTADO (2026-06-21, already solved better by the fork). The fork's recent `4803abc` already implements all three parts of this fix, and more robustly: `resolveMonoFontFamily`/`buildFontStack` always back a custom family with the platform default mono stack (ending in `monospace`); `normalizeFontFamilies` quotes space-containing names and handles comma stacks (needed for the WebGL canvas atlas, beyond the upstream's `resolveFontFamily`); and the fork's `FontFamilyInput` already commits on blur/Enter with a local draft so spaces can be typed mid-edit. The fork also covers the editor font, which the upstream does not. No code change needed.
- Rationale: rendering correctness for custom fonts, already satisfied by the fork's font system.

### Perf: drop tokenlens dep and deduplicate woff fonts (partial)

- Files: `package.json`, `pnpm-lock.yaml`, `src/styles/fonts.css`, `src/main.tsx` (DIVERGED). EXCLUDED upstream paths: `src/components/ai-elements/context.tsx`, `src/modules/ai/components/AiMiniWindow.tsx`, `src/settings/main.tsx` AI imports (all absent in the fork).
- Upstream commits: `cbe1ad4`
- Divergence: PARTIAL. Manual edit only.
- Instructions: confirm whether `tokenlens` is even a dependency of the fork (it may already be absent since it was AI-adjacent). If present, remove it. Apply the woff de-duplication in `fonts.css`. Skip all `ai-elements`/`AiMiniWindow` hunks. Run `pnpm eager-budget.test` (the fork guards eager bundle size).
- Rationale: bundle size, core fork value. Verify the eager-budget test still passes.

## New features to evaluate (Bucket C)

### Editor: more syntax highlighting + all Dockerfile variants + Vue

- Description: extends `languageResolver.ts` with many more language modes (all Dockerfile variants, more formats) and Vue (`.vue`). `b656fe3` removes the now-duplicate vue/swift loaders introduced earlier.
- Files: `src/modules/editor/lib/languageResolver.ts`
- Upstream commits: `b7eb8d6`, `90aeab5` (Vue), `b656fe3` (dedupe vue/swift)
- Divergence: fork CLEAN; inverse diff (118 lines) matches the upstream chain. Cherry-pick viable.
- Recommendation: ADD. Low risk, pure language-mode additions, no new heavy deps expected (verify no new CodeMirror language packages bloat the bundle; if they do, reconsider per the lightweight-bundle goal).
- Approach if accepted: `git cherry-pick 90aeab5 b7eb8d6 b656fe3` in upstream chronological order; resolve any ordering conflicts. Re-run `pnpm check-types` and the eager-budget test.
- Decision state: EVALUAR

### Terminal: OSC 52 clipboard handler

- Description: adds an OSC 52 handler so terminal applications can write to the system clipboard.
- Files: `src/modules/terminal/lib/osc-handlers.ts`, `src/modules/terminal/lib/useTerminalSession.ts`, `osc-handlers.test.ts`
- Upstream commits: `0c647c4`
- Divergence: REQUIRES MANUAL MERGE (both files diverged).
- Recommendation: EVALUAR. Security implication: OSC 52 lets any process writing to the PTY (including remote content over SSH) place data on the host clipboard. Decide whether to gate it behind a setting (default off) or accept the upstream's always-on behavior. Many terminals expose read-OSC-52 as opt-in for this reason; confirm this handler is write-only.
- Approach if accepted: port the handler into the fork's `osc-handlers.ts`, adapt the test, and (recommended) add a preferences gate.
- Decision state: EVALUAR

### Window: confirm quit while a terminal process is running

- Description: shows a confirmation dialog on app quit if a foreground terminal process is still running.
- Files: `src/app/hooks/useAppCloseGuard.ts` (new), `src/app/components/CloseDialogs.tsx` (DIVERGED 7), `src/app/App.tsx` (DIVERGED 110)
- Upstream commits: `d782f7d`
- Divergence: REQUIRES MANUAL MERGE. The fork already has `CloseDialogs.tsx`, `closeQueue.ts`, and `useTabCloseGuards.ts`, so the close-guard machinery exists and the upstream hook must be reconciled with it rather than dropped in.
- Recommendation: EVALUAR. Good UX fit, but the fork's close path (closeQueue + tab guards) differs from the upstream's; decide whether to add app-level quit guard on top of the existing per-tab guards or fold it in.
- Approach if accepted: study the fork's `closeQueue.ts` + `useTabCloseGuards.ts`, then add the app-quit guard consistently (likely a new `useAppCloseGuard.ts` adapted to the fork's process-running detection).
- Decision state: EVALUAR

### Editor: standalone editor theme picker + more themes

- Description: decouples the editor (CodeMirror) theme from the app theme, adds a picker in Settings, and ships new themes (dracula, everforest, kanagawa, kanagawa-dragon, solarized).
- Files: 21 files, 866 insertions. New: `editor/lib/cmThemes.ts`, `editor/lib/useEditorThemeExt.ts`, `theme/resolveEditorTheme.ts` (+ test), 5 new theme files. Modified: `editor/lib/themes.ts` (CLEAN), `settings/store.ts` (DIVERGED 23), `ThemeProvider.tsx`, `ThemesSection.tsx`, `AiDiffPane.tsx` (ABSENT here), `EditorPane.tsx`, `GitDiffPane.tsx`, `catppuccin.ts`, `rose-pine.ts`.
- Upstream commits: `35f8711`, plus `1afe8d5` (a11y/complexity lint fixes on `ThemesSection.tsx` + `tokens.ts`, depends on this).
- Divergence: MANUAL MERGE. Touches `AiDiffPane.tsx` which does not exist in the fork (drop that hunk). `settings/store.ts` diverged.
- Recommendation: EVALUAR. Sizeable but self-contained feature with clear user value. Cost check: the new themes are plain data objects (cheap); confirm `cmThemes.ts` does not pull a heavy CodeMirror theme dependency.
- Approach if accepted: port `resolveEditorTheme.ts` + tests, `cmThemes.ts`, `useEditorThemeExt.ts`, the 5 theme files, and the `settings/store.ts` keys; wire the picker into the fork's `ThemesSection.tsx`; apply the editor-theme extension in `EditorPane.tsx` and `GitDiffPane.tsx` (skip `AiDiffPane.tsx`). Then apply `1afe8d5` on top.
- Decision state: EVALUAR

### WSL: restore per-space workspace env on reopen (partial)

- Description: persists and restores the per-environment (WSL distro / Local) workspace env when reopening. Upstream ties it to "spaces"; the fork has no spaces.
- Files: `src-tauri/src/modules/workspace.rs` (DIVERGED 6), `src-tauri/src/modules/pty/mod.rs` (DIVERGED 7), `src/app/App.tsx`, `useWorkspaceSwitcher.ts`. EXCLUDED: `src/modules/spaces/lib/*` (absent).
- Upstream commits: `4a6d803`
- Divergence: MANUAL MERGE / EXTRACT. The Rust side (`workspace.rs` env restore) may be valuable independent of the spaces layer; the frontend half is spaces-coupled.
- Recommendation: EVALUAR. Extract only the non-spaces portion (workspace env persistence in `workspace.rs`) if it maps onto the fork's Workspace environment switching (`src/modules/workspace/`). If it cannot be cleanly separated from the spaces model, skip and document.
- Decision state: EVALUAR

### Dependabot bumps

- Description: dependency bumps. `6dc2648` actions/checkout 6->7; `53c3a60` + `5a656c3` npm-dev groups; `deded91` npm-prod-minor-patch (15 updates); `1142fba` cargo group.
- Recommendation: EVALUAR per bump. Do NOT cherry-pick: the fork's `package.json` / `Cargo.toml` diverge (AI deps removed), so an upstream lockfile bump will not apply. Instead, re-run the equivalent bump natively in the fork (`pnpm update <pkg>`, `cargo update -p <pkg>`) for packages the fork actually has. `6dc2648` (GitHub Action) can apply directly to the fork's workflow if the workflow exists. Skip bumps that only touch AI packages.
- Decision state: EVALUAR

## Execution steps

Run in this order, committing each completed chunk before the next (commit-barrier discipline; never let sync edits sit next to unrelated WIP).

1. Confirm a clean working tree (`git status --porcelain` empty). If not, stop.
2. Cherry-pick the clean, low-risk fix: `git cherry-pick 0eac4a6` (git stage). Run `cd src-tauri && cargo clippy && cargo test --locked`.
3. Manual-merge the terminal pre-attach buffer (`7f972aa` + `a1fca84`) into `useTerminalSession.ts`. Test, commit `upstream: buffer input typed before the pty attaches, bounded`.
4. Manual-merge PTY reap-on-child-exit (`1691e73`) into `session.rs`. Add/keep a test. Commit.
5. Manual-merge fish + starship cwd (`2711841`) into `init.fish`. Commit.
6. Manual-merge custom-font monospace fallback (`6019e04`) into `fonts.ts` / `fonts.css`, reconciling with fork commit `4803abc`. Adapt `fonts.test.ts`. Commit.
7. Manual-merge explorer close-menu-after-delete (`7baee7c`). Commit.
8. Partial perf: drop `tokenlens` if present + woff dedup (`cbe1ad4`), skipping AI hunks. Run eager-budget test. Commit.
9. For each Bucket C EVALUAR item: get the user's decision, then apply per its "Approach if accepted" and update its Decision state in this file (APLICADO / DESCARTADO / POSPUESTO with date and reason).
10. Run the full quality suite (below). Fix everything before the documentation step.
11. Update `docs/FORK.md` (see below).

## Quality checks (run after all changes)

```bash
pnpm lint
pnpm check-types
pnpm test
cd src-tauri && cargo clippy && cargo test --locked
```

Fix all errors before proceeding to the documentation step. Do not skip or bypass checks.

## FORK.md updates (always last)

Append to `### Upstream sync log` in `docs/FORK.md`:

```markdown
#### 2026-06-21

- Upstream HEAD: b656fe3816d0ddaefb17e7acff5e16889f6e939a
- Commits reviewed: 8e1c4743fb4efcf1f3d089457c32dc1326552683..b656fe3816d0ddaefb17e7acff5e16889f6e939a (34 commits, 0 merges)
- Outcome: work plan created at docs/upstream-2026-06-21.md
- Changes applied: none -- pending plan execution. (When executed, update this entry with the actual applied/skipped/rejected lists.)
- Changes skipped (removed surface): AI (3 commits: Grok/Opus/GPT models, Groq/Whisper STT, microphone entitlement); Spaces/Tabs (6 commits: MRU switcher, tab drag-reorder, switcher HUD, per-space close dialog); upstream infra (CodeRabbit config, v0.8.1 bump, README badges, Nix sources).
- Note: the upstream did NOT touch the git history module in this range; the only git change is the staging fix (0eac4a6).
```

If any Bucket C item is rejected by the user during execution, also add it under the relevant "What has been removed" subsection of `docs/FORK.md` with a note that it was evaluated during the 2026-06-21 sync and why it was rejected.
