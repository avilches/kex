# Upstream sync plan -- 2026-06-24

## Context

Fork: terax-ai / Kex (no AI subsystem, enhanced workspace/pane model)
Upstream: https://github.com/crynta/terax-ai.git
Fork philosophy: no AI runtime, no API keys, no keychain; focus on clean fast terminal workspace

Last synced upstream commit: 8043cfa106f7de9ee182b3dbd051e35cd2f1b273
Upstream HEAD at plan creation: d77476e762b8ade438c643061723b9f494213600
Commits to process: 8043cfa..d77476e (10 commits reales, 0 merges)

## Removed surface (skip all changes to these paths)

- `src/modules/ai/` -- entire AI subsystem
- `src/modules/agents/store/managedAgentsStore.ts` -- managed agents store
- `src/modules/tabs/` -- flat tab model (removed in Phase 2, replaced by Workspace/Pane/Panel)
- `src/modules/editor/AiDiffPane.tsx` -- AI diff pane
- `src/modules/editor/lib/autocomplete/inlineExtension.ts` -- AI autocomplete CodeMirror extension
- `src/settings/sections/ModelsSection.tsx` -- AI models settings section
- `src-tauri/src/modules/net.rs` -- HTTP proxy for AI calls
- `src-tauri/src/modules/secrets.rs` -- OS keychain for API keys
- `nix/` -- Nix packaging (fork does not distribute via Nix)

## Ignored changes (Bucket A)

- `564e145` nix: update sources to 0.8.2 -- ignorado, fork no usa Nix
- `63099f5` chore: v0.8.2 -- version bump propio del upstream (tauri.conf.json, Cargo.toml); fork tiene su propio versionado
- `db63229` build(deps): bump npm-prod-minor-patch -- re-derivar nativamente con `pnpm update`, no cherry-pick
- `a8b9481` feat(autocomplete): named openai-compatible endpoints -- 100% superficie eliminada (inlineExtension.ts, ModelsSection.tsx, partes AI de EditorPane.tsx)
- `7a812ca` fix(window): quit on last-tab shell exit -- descartado; el fork no tiene `respawnSession` ni el modelo de tabs que este commit modifica; los panes vacios ya muestran pantalla de bienvenida

## Already applied (no action needed)

- `414ee17` fix(window): grant `core:window:allow-destroy` permission -- ya presente en el fork (anadido por `8ba7232`); no hacer nada

---

## Changes to apply

### B1 -- fix(breadcrumb): scroll horizontal para paths largos

- Commit: `6963d82`
- Files: `src/components/ui/breadcrumb.tsx`
- El fork NO ha modificado este archivo desde el ultimo sync.
- Approach: cherry-pick limpio

**Execution:**
```bash
git cherry-pick 6963d82
```

Verifica que el commit aplica sin conflictos. El cambio es 2 lineas CSS:
- `<nav>`: anade clase `min-w-0`
- `<ol>`: cambia `flex-wrap` a `flex-nowrap overflow-x-auto w-full whitespace-nowrap`, elimina `wrap-break-word`

---

### C1 -- feat(terminal): font weight del terminal

- Commit: `9fc0425`
- Files: `store.ts`, `rendererPool.ts`, `useTerminalSession.ts`, `GeneralSection.tsx` (todos con divergencia en el fork), mas `coerceFontWeight.test.ts` (nuevo)
- Approach: merge manual archivo por archivo

**Execution:**

**1. Crear `src/modules/settings/coerceFontWeight.test.ts`** (archivo nuevo, copia exacta del upstream):

```typescript
import { describe, expect, it } from "vitest";
import { coerceFontWeight } from "./store";

describe("coerceFontWeight", () => {
  it("keeps supported weights", () => {
    for (const w of ["normal", "500", "600", "bold"]) {
      expect(coerceFontWeight(w)).toBe(w);
    }
  });

  it("trims surrounding whitespace", () => {
    expect(coerceFontWeight("  bold  ")).toBe("bold");
  });

  it("falls back to normal for unsupported or empty values", () => {
    expect(coerceFontWeight("")).toBe("normal");
    expect(coerceFontWeight("900")).toBe("normal");
    expect(coerceFontWeight("heavy")).toBe("normal");
  });
});
```

**2. Editar `src/modules/settings/store.ts`** -- localizar la zona de las preferencias de fuente del terminal y anadir:

En el tipo `Preferences`, junto a `terminalLetterSpacing`:
```typescript
terminalFontWeight: string;
```

En las constantes KEY_*, junto a `KEY_TERMINAL_LETTER_SPACING`:
```typescript
const KEY_TERMINAL_FONT_WEIGHT = "terminalFontWeight";
```

En `DEFAULT_PREFERENCES`, junto a `terminalLetterSpacing`:
```typescript
terminalFontWeight: "normal",
```

Funcion `coerceFontWeight` (anadir como funcion exportada, junto a `setTerminalLetterSpacing`):
```typescript
const TERMINAL_FONT_WEIGHT_VALUES = new Set(["normal", "500", "600", "bold"]);

export function coerceFontWeight(value: string): string {
  const v = value.trim();
  return TERMINAL_FONT_WEIGHT_VALUES.has(v) ? v : "normal";
}

export async function setTerminalFontWeight(value: string): Promise<void> {
  await writePref(KEY_TERMINAL_FONT_WEIGHT, coerceFontWeight(value));
}
```

En `loadPreferences()`, junto al bloque de `terminalLetterSpacing`:
```typescript
terminalFontWeight: coerceFontWeight(
  get<string>(KEY_TERMINAL_FONT_WEIGHT) ??
    DEFAULT_PREFERENCES.terminalFontWeight,
),
```

En `onPreferencesChange`, en el mapa de claves a campos, junto a `terminalLetterSpacing`:
```typescript
[KEY_TERMINAL_FONT_WEIGHT]: "terminalFontWeight",
```

**3. Editar `src/modules/terminal/lib/rendererPool.ts`**:

En el import de `@xterm/xterm`, anadir `FontWeight`:
```typescript
import { type FontWeight, Terminal } from "@xterm/xterm";
```

En `termOptions()`, junto a `letterSpacing`:
```typescript
fontWeight: prefs.terminalFontWeight as FontWeight,
```

Anadir funcion `applyFontWeight` (junto a `applyLetterSpacing`):
```typescript
export function applyFontWeight(weight: string): void {
  for (const slot of slots) {
    if (slot.term.options.fontWeight === weight) continue;
    slot.term.options.fontWeight = weight as FontWeight;
  }
}
```

**4. Editar `src/modules/terminal/lib/useTerminalSession.ts`**:

En el import de `rendererPool`, anadir `applyFontWeight`:
```typescript
import {
  applyFontWeight,
  applyLetterSpacing,
  ...
} from "./rendererPool";
```

Anadir `useEffect` para font weight, junto al de `letterSpacing`:
```typescript
const fontWeight = usePreferencesStore((p) => p.terminalFontWeight);
useEffect(() => {
  applyFontWeight(fontWeight);
}, [fontWeight]);
```

**5. Editar `src/settings/sections/GeneralSection.tsx`**:

Anadir import `setTerminalFontWeight` desde el store.

Anadir constante junto a las demas constantes de terminal:
```typescript
const TERMINAL_FONT_WEIGHTS = [
  { value: "normal", label: "Normal" },
  { value: "500", label: "Medium" },
  { value: "600", label: "Semi-Bold" },
  { value: "bold", label: "Bold" },
] as const;
```

En el cuerpo de `GeneralSection`, anadir estado:
```typescript
const terminalFontWeight = usePreferencesStore((s) => s.terminalFontWeight);
```

Anadir `<SettingRow>` junto al de Letter spacing:
```tsx
<SettingRow
  title="Font weight"
  description="Thickness of terminal characters"
>
  <Select
    value={terminalFontWeight}
    onValueChange={(v) => void setTerminalFontWeight(v)}
  >
    <SelectTrigger value={terminalFontWeight} className="h-8 w-28 text-[12px]">
      <SelectValue />
    </SelectTrigger>
    <SelectContent>
      {TERMINAL_FONT_WEIGHTS.map((w) => (
        <SelectItem key={w.value} value={w.value} className="text-[12px]">
          {w.label}
        </SelectItem>
      ))}
    </SelectContent>
  </Select>
</SettingRow>
```

**Commit tras C1:**
```
git add src/modules/settings/coerceFontWeight.test.ts \
        src/modules/settings/store.ts \
        src/modules/terminal/lib/rendererPool.ts \
        src/modules/terminal/lib/useTerminalSession.ts \
        src/settings/sections/GeneralSection.tsx
git commit -m "upstream: add terminal font weight setting"
```

---

### C2 -- feat(terminal): selector de shell por defecto

- Commit: `a770307`
- Files: Rust (`lib.rs`, `pty/mod.rs`, `pty/session.rs`, `pty/shell_init.rs`) + Frontend (`store.ts`, `pty-bridge.ts`, `useTerminalSession.ts`, `osc-handlers.ts`, `osc-handlers.test.ts`, `GeneralSection.tsx`)
- Todos con divergencia en el fork
- Approach: merge manual archivo por archivo

**IMPORTANTE antes de editar cada archivo:** leerlo primero completo para entender el contexto actual del fork.

**Execution -- Rust:**

**1. Editar `src-tauri/src/modules/pty/shell_init.rs`**:

Anadir `ShellInfo` struct publico (arriba del archivo, antes de `build_command`). Nota: el upstream tiene un comentario que menciona "Terax"; adaptarlo a "Kex":

```rust
#[derive(serde::Serialize)]
pub struct ShellInfo {
    pub name: String,
    pub path: String,
    /// True when Kex injects OSC 7/133 integration for this shell (cwd
    /// tracking, command blocks, agent detection). Others spawn bare.
    pub integrated: bool,
}

pub fn list_shells() -> Vec<ShellInfo> {
    #[cfg(unix)]
    {
        unix::list_shells()
    }
    #[cfg(windows)]
    {
        windows::list_shells()
    }
}
```

Anadir funcion de seguridad `sanitize_shell_override` (validacion de whitelist):

```rust
fn sanitize_shell_override(shell: Option<String>) -> Option<String> {
    let candidate = shell.map(|s| s.trim().to_string()).filter(|s| !s.is_empty())?;
    let target = std::fs::canonicalize(&candidate).ok();
    let allowed = list_shells().into_iter().any(|s| {
        s.path == candidate || (target.is_some() && std::fs::canonicalize(&s.path).ok() == target)
    });
    if allowed {
        Some(candidate)
    } else {
        log::warn!("ignoring non-enumerated shell override '{candidate}'");
        None
    }
}
```

Modificar la firma de `build_command` para aceptar `shell: Option<String>` y pasarlo a las plataformas:
```rust
pub fn build_command(
    cwd: Option<String>,
    workspace: WorkspaceEnv,
    blocks: bool,
    shell: Option<String>,
) -> Result<CommandBuilder, String> {
    let shell = sanitize_shell_override(shell);
    #[cfg(unix)]
    { unix::build(cwd, blocks, shell) }
    #[cfg(windows)]
    { windows::build(cwd, workspace, blocks, shell) }
}
```

En el modulo `unix`:

Refactorizar `Shell::detect()` extrayendo `Shell::classify(path: &str) -> Shell`:
```rust
pub fn classify(path: &str) -> Shell {
    match path.rsplit('/').next().unwrap_or("") {
        "zsh" => Shell::Zsh,
        "bash" => Shell::Bash,
        "fish" => Shell::Fish,
        _ => Shell::Other,
    }
}
```

Anadir `Shell::resolve(shell_override: Option<String>) -> (Shell, String)`:
```rust
pub fn resolve(shell_override: Option<String>) -> (Shell, String) {
    if let Some(path) = shell_override
        .map(|s| s.trim().to_string())
        .filter(|s| !s.is_empty())
    {
        if Path::new(&path).is_file() {
            return (Self::classify(&path), path);
        }
        log::warn!("configured shell '{path}' not found, using auto-detect");
    }
    Self::detect()
}
```

Modificar `unix::build` para aceptar y usar `shell_override`:
```rust
pub fn build(cwd: Option<String>, blocks: bool, shell_override: Option<String>) -> Result<CommandBuilder, String> {
    let (shell, shell_path) = Shell::resolve(shell_override);
    // ... resto sin cambios
}
```

Anadir `unix::list_shells()` que lee `/etc/shells` y devuelve `Vec<ShellInfo>` con los shells disponibles en el sistema (ver implementacion upstream).

Anadir tests en el modulo unix (los 4 tests del upstream: `classify_maps_known_shells`, `resolve_uses_an_existing_override`, `resolve_falls_back_when_override_missing`, `resolve_falls_back_on_empty_override`).

En el modulo `windows`:

Modificar `windows::build` para aceptar `shell: Option<String>` y respetar el override (ver diff upstream). Anadir soporte para git-bash (`bash.exe` desde `git_bash_path()`): set `CHERE_INVOKING=1` y usar el rcfile de bash con ruta forward-slash. Anadir `windows::list_shells()` y `git_bash_path()`.

Anadir tests top-level (los 2 tests del upstream: `rejects_non_enumerated_override`, `empty_or_missing_override_is_none`).

**2. Editar `src-tauri/src/modules/pty/mod.rs`**:

En `pty_open`, anadir parametro `shell: Option<String>` y pasarlo a `session::spawn`:
```rust
pub async fn pty_open(
    ...,
    shell: Option<String>,
    on_data: Channel<Response>,
    on_exit: Channel<i32>,
) -> Result<u32, String> {
    // ...
    session::spawn(id, app, cols, rows, cwd, workspace, blocks, shell, on_data, on_exit)
```

Anadir comando `pty_list_shells` al final del archivo:
```rust
#[tauri::command]
pub fn pty_list_shells() -> Vec<shell_init::ShellInfo> {
    shell_init::list_shells()
}
```

**3. Editar `src-tauri/src/modules/pty/session.rs`**:

En `spawn`, anadir `shell: Option<String>` y pasarlo a `build_command`:
```rust
pub fn spawn(
    ...,
    shell: Option<String>,
    ...
) -> Result<(Arc<Session>, PtySize), String> {
    // ...
    let cmd = shell_init::build_command(cwd, workspace, blocks, shell)?;
```

**4. Editar `src-tauri/src/lib.rs`**:

En `tauri::generate_handler![]`, anadir `pty::pty_list_shells`:
```rust
pty::pty_list_shells,
pty::pty_open,  // junto a este
```

**Execution -- Frontend:**

**5. Editar `src/modules/settings/store.ts`**:

Anadir en el tipo `Preferences` (junto a `terminalFontFamily`):
```typescript
terminalShell: string;
```

Anadir constante KEY (junto a `KEY_TERMINAL_FONT_FAMILY`):
```typescript
const KEY_TERMINAL_SHELL = "terminalShell";
```

Anadir en `DEFAULT_PREFERENCES` (junto a `terminalFontFamily`):
```typescript
terminalShell: "",
```

En `loadPreferences()` (junto al bloque de `terminalFontFamily`):
```typescript
terminalShell: get<string>(KEY_TERMINAL_SHELL) ?? DEFAULT_PREFERENCES.terminalShell,
```

Anadir funcion `setTerminalShell` (junto a `setTerminalFontFamily`):
```typescript
export async function setTerminalShell(value: string): Promise<void> {
  await writePref(KEY_TERMINAL_SHELL, value);
}
```

En `onPreferencesChange` (junto a `KEY_TERMINAL_FONT_FAMILY`):
```typescript
[KEY_TERMINAL_SHELL]: "terminalShell",
```

**6. Editar `src/modules/terminal/lib/pty-bridge.ts`**:

Anadir `shell?: string` a los parametros de `openPty` (al final, despues de `panelId?`):
```typescript
export async function openPty(
  cols: number,
  rows: number,
  handlers: PtyHandlers,
  cwd?: string,
  blocks?: boolean,
  panelId?: string,
  shell?: string,
): Promise<PtySession> {
```

En el invoke `pty_open`, anadir `shell: shell ?? null`:
```typescript
const id = await invoke<number>("pty_open", {
  cols,
  rows,
  cwd: cwd ?? null,
  workspace: currentWorkspaceEnv(),
  blocks: blocks ?? false,
  panelId: panelId ?? null,
  shell: shell ?? null,
  onData,
  onExit,
});
```

**7. Editar `src/modules/terminal/lib/useTerminalSession.ts`**:

En `openPtyForSession`, pasar el shell configurado como ultimo argumento a `openPty`:
```typescript
return openPty(
  startCols,
  startRows,
  { ... },
  cwd,
  s.blocks,
  leafId,
  usePreferencesStore.getState().terminalShell || undefined,
);
```

**8. Editar `src/modules/terminal/lib/osc-handlers.ts`**:

Anadir import al principio:
```typescript
import { IS_WINDOWS } from "@/lib/platform";
```

En `parseOsc7`, expandir el bloque de path Windows para incluir git-bash (MSYS):
```typescript
if (/^\/[A-Za-z]:/.test(path)) {
  path = path.slice(1);
} else if (IS_WINDOWS) {
  // git-bash (MSYS) reports cwd as /c/Users/foo; map it to C:/Users/foo.
  const drive = path.match(/^\/([A-Za-z])(\/.*)?$/);
  if (drive) path = `${drive[1].toUpperCase()}:${drive[2] ?? "/"}`;
}
```

**9. Editar `src/modules/terminal/lib/osc-handlers.test.ts`**:

Al principio del archivo, tras los imports pero antes del primer `describe`, anadir el mock de plataforma y el nuevo test:

```typescript
// git-bash path mapping is Windows-only; exercise that branch.
vi.mock("@/lib/platform", () => ({ IS_WINDOWS: true }));
```

Dentro del describe "OSC 7 cwd handler", anadir el test de git-bash:
```typescript
it("maps git-bash /c/ cwd to a Windows drive path", () => {
  const { term, handlers } = makeFakeTerm();
  const state = createShellIntegrationState();
  const onCwd = vi.fn();
  registerPromptTracker(term, state);
  registerCwdHandler(term, onCwd, state);

  handlers.get(133)?.("A");
  handlers.get(7)?.("file:///c/Users/leo/project");

  expect(onCwd).toHaveBeenCalledWith("C:/Users/leo/project");
});
```

NOTA: el mock `vi.mock("@/lib/platform", ...)` afecta a todos los tests del archivo porque es un mock de modulo. Verificar que los tests existentes de mapeo de paths Windows (si los hay) no se rompen; si el archivo ya tenia un mock de plataforma, unificarlo.

**10. Editar `src/settings/sections/GeneralSection.tsx`**:

Anadir imports:
```typescript
import { invoke } from "@tauri-apps/api/core";
import { useEffect, useState } from "react";
import { setTerminalShell, ... } from "@/modules/settings/store";
```

Anadir tipo y constante (fuera del componente):
```typescript
type ShellInfo = { name: string; path: string; integrated: boolean };
const SHELL_AUTO = "auto";
```

En el componente, anadir estado:
```typescript
const terminalShell = usePreferencesStore((s) => s.terminalShell);
const [shells, setShells] = useState<ShellInfo[]>([]);
```

Anadir `useEffect` para cargar la lista de shells:
```typescript
useEffect(() => {
  void invoke<ShellInfo[]>("pty_list_shells")
    .then(setShells)
    .catch(() => {});
}, []);
```

Anadir `<SettingRow>` en la seccion Terminal, junto a los otros ajustes de fuente:
```tsx
<SettingRow
  title="Default shell"
  description={
    shells.find((s) => s.path === terminalShell)?.integrated === false
      ? "Command blocks and directory tracking are unavailable for this shell."
      : "Shell for new terminal tabs. Existing tabs keep their shell."
  }
>
  <Select
    value={terminalShell || SHELL_AUTO}
    onValueChange={(v) => void setTerminalShell(v === SHELL_AUTO ? "" : v)}
  >
    <SelectTrigger value={terminalShell || SHELL_AUTO} className="h-8 w-40 text-[12px]">
      <SelectValue />
    </SelectTrigger>
    <SelectContent>
      <SelectItem value={SHELL_AUTO} className="text-[12px]">Auto</SelectItem>
      {shells.map((s) => (
        <SelectItem key={s.path} value={s.path} className="text-[12px]">
          {s.name}
        </SelectItem>
      ))}
    </SelectContent>
  </Select>
</SettingRow>
```

**Commit tras C2:**
```bash
git add src-tauri/src/lib.rs \
        src-tauri/src/modules/pty/mod.rs \
        src-tauri/src/modules/pty/session.rs \
        src-tauri/src/modules/pty/shell_init.rs \
        src/modules/settings/store.ts \
        src/modules/terminal/lib/pty-bridge.ts \
        src/modules/terminal/lib/useTerminalSession.ts \
        src/modules/terminal/lib/osc-handlers.ts \
        src/modules/terminal/lib/osc-handlers.test.ts \
        src/settings/sections/GeneralSection.tsx
git commit -m "upstream: add user-selectable default shell"
```

---

### C3 -- feat(editor): selector de lenguaje por panel

- Commit: `d77476e`
- Partes de superficie eliminada a ignorar: `TabBar.tsx`, `useTabs.ts`, `AiDiffPane.tsx`
- El resto se integra en el modelo del fork
- Approach: archivos nuevos copiados del upstream + merge manual de los existentes + reimplementacion de UI

**Execution -- Core logic:**

**1. Crear `src/modules/editor/lib/languageDefinitions.ts`** (archivo nuevo):

Copiar el contenido completo desde el upstream (`git show d77476e:src/modules/editor/lib/languageDefinitions.ts`).

IMPORTANTE: El fork tiene `.kex-theme` en lugar de `.terax-theme`. Anadir al array `LANGUAGES` una entrada para los archivos `.kex-theme` (con JSON + colorSwatches), ya que el nuevo refactor de `EditorPane.tsx` elimina el case especial que existia para estos archivos:

```typescript
{
  name: "Kex Theme",
  extensions: ["kex-theme"],
  loader: async () => {
    const [{ json }, { colorSwatches }] = await Promise.all([
      import("@codemirror/lang-json"),
      import("./colorSwatches"),
    ]);
    return [json(), colorSwatches()];
  },
  userSelectable: false,
},
```

**2. Crear `src/modules/editor/lib/languageResolver.test.ts`** (archivo nuevo):

Copiar directamente desde el upstream (`git show d77476e:src/modules/editor/lib/languageResolver.test.ts`).

**3. Reemplazar `src/modules/editor/lib/languageResolver.ts`**:

El upstream refactoriza completamente este archivo (292 lineas eliminadas, 65 nuevas). La nueva version usa el catalogo de `languageDefinitions.ts` en lugar de la tabla `loaders` inline. Lee el archivo actual del fork primero para verificar que no haya customizaciones fork-especificas fuera de la tabla de loaders.

Aplicar el refactor del upstream: `git show d77476e -- src/modules/editor/lib/languageResolver.ts` para ver el diff completo. El resultado exporta `LanguageResult` (con campos `ext`, `name`, `id`) y la funcion `resolveLanguage(path) => Promise<LanguageResult | null>`.

**4. Editar `src/modules/editor/EditorPane.tsx`**:

a) Aplicar los cambios del upstream al tipo `Props`/`EditorPaneHandle`:
```typescript
overrideLanguage?: string | null;
```

b) En el `useEffect` de resolucion de lenguaje, sustituir el case especial `.kex-theme` y el return de `Extension` por el nuevo flujo de `LanguageResult`:

```typescript
// Antes:
const resolve = async (): Promise<Extension> => {
  if (path.toLowerCase().endsWith(".kex-theme")) {
    // ... case especial
  }
  return (await resolveLanguage(path)) ?? [];
};
void resolve().then((extension) => {
  // ...
  view.dispatch({ effects: languageCompartment.reconfigure(extension) });
});

// Despues (segun upstream adaptado al fork):
const resolve = async (): Promise<LanguageResult> => {
  const resolvePath = overrideLanguage ? `dummy.${overrideLanguage}` : path;
  return (await resolveLanguage(resolvePath)) ?? { ext: [], name: "", id: "" };
};
void resolve().then((result) => {
  if (cancelled) return;
  if (result.id) languageRef.current = result.id;
  const view = cmRef.current?.view;
  if (!view) return;
  view.dispatch({ effects: languageCompartment.reconfigure(result.ext) });
  // Notificar al exterior el nombre del lenguaje resuelto:
  onLanguageResolved?.(result.name);
});
```

c) Anadir `onLanguageResolved?: (name: string) => void` a las Props (para que la UI pueda mostrar el nombre del lenguaje activo sin guardar estado en el panel).

d) Actualizar la dependencia del `useEffect`: `[path, doc.status, overrideLanguage]`.

**5. Editar `src/modules/editor/GitDiffPane.tsx`**:

Adaptar los dos puntos donde se usa `resolveLanguage` para usar el nuevo `LanguageResult`:

```typescript
// En la inicializacion:
languageCompartment.of(initialLang?.ext ?? []),

// En el useEffect:
resolveLanguage(path).then((res) => {
  // ...
  view.dispatch({ effects: languageCompartment.reconfigure(res?.ext ?? []) });
});
```

**Execution -- Data model:**

**6. Editar `src/modules/workspaces/lib/types.ts`**:

En la variante `editor` del tipo `Panel`, anadir campo opcional:
```typescript
| { id: string; kind: "editor"; path: string; title?: string; dirty: boolean; preview: boolean; locked?: boolean; autofocus?: boolean; overrideLanguage?: string | null }
```

**7. Editar `useWorkspaces`** (el store/hook principal de workspaces -- leer primero para localizar donde viven las acciones de mutacion de panel):

Anadir accion `setOverrideLanguage(panelId: string, lang: string | null): void` que encuentra el panel editor por id en el workspace activo y actualiza su campo `overrideLanguage`. Seguir el patron de otras acciones de mutacion de panel del mismo store.

**8. Editar `src/modules/editor/EditorStack.tsx`**:

Pasar `overrideLanguage` del panel a `EditorPane`:
```tsx
<EditorPane
  ref={...}
  overrideLanguage={panel.overrideLanguage}
  onLanguageResolved={...}  // ver paso UI
  ...
/>
```

**Execution -- UI (reimplementacion fork-especifica):**

En el upstream, el selector de lenguaje vive en `TabBar.tsx` (eliminado en el fork). En el fork, el lugar natural es `EditorOverlayBar` (la barra flotante `[...]` que ya existe para los paneles editor).

**9. Anadir selector de lenguaje a `EditorOverlayBar`**:

`EditorOverlayBar` recibe props del panel activo. Anadir:
- Prop `overrideLanguage?: string | null` -- el override actual
- Prop `onLanguageChange?: (lang: string | null) => void` -- callback para cambiar
- Prop `currentLanguageName?: string` -- nombre del lenguaje actualmente resuelto (viene de `onLanguageResolved` del EditorPane via el padre)

Implementar un boton que muestra el nombre del lenguaje activo (o "Auto" si no hay override) y al hacer clic abre un `<Popover>` con un `<Command>` (searchable combobox) listando todas las entradas de `LANGUAGES` donde `userSelectable !== false`. Seleccionar un idioma llama `onLanguageChange(lang.id ?? lang.extensions[0])`. Un boton "Auto" al principio llama `onLanguageChange(null)` para limpiar el override.

El componente `<Command>` / `<CommandInput>` / `<CommandList>` de shadcn/ui es el patron estandar para este tipo de picker buscable.

**10. Conectar en el componente padre de `EditorOverlayBar`** (leer el codigo actual para identificar quien renderiza `EditorOverlayBar`):

- Pasar `overrideLanguage={panel.overrideLanguage}` y `onLanguageChange={(lang) => setOverrideLanguage(panel.id, lang)}`
- Mantener estado local `currentLanguageName` que se actualiza via el callback `onLanguageResolved` de `EditorPane`

**Commit tras C3:**
```bash
git add src/modules/editor/lib/languageDefinitions.ts \
        src/modules/editor/lib/languageResolver.test.ts \
        src/modules/editor/lib/languageResolver.ts \
        src/modules/editor/EditorPane.tsx \
        src/modules/editor/EditorStack.tsx \
        src/modules/editor/GitDiffPane.tsx \
        src/modules/workspaces/lib/types.ts \
        src/modules/workspaces/...   # el store/hook donde vive setOverrideLanguage
        src/modules/editor/EditorOverlayBar.tsx
git commit -m "upstream: add per-panel language override with selector UI"
```

---

## Quality checks (run after all changes)

```bash
pnpm exec biome lint ./src
pnpm check-types
pnpm test
cd src-tauri && cargo clippy && cargo test --locked
```

Corregir todos los errores antes de pasar a la actualizacion de FORK.md. No omitir ni puentear los checks.

---

## FORK.md updates (always last)

Actualizar `docs/FORK.md`:

**1. Crear nueva entrada en `### Upstream sync log`** al final del archivo:

```markdown
#### 2026-06-24

- Upstream HEAD: d77476e762b8ade438c643061723b9f494213600
- Commits revisados: 8043cfa106f7de9ee182b3dbd051e35cd2f1b273..d77476e762b8ade438c643061723b9f494213600 (10 commits reales, 0 merges)
- Outcome: work plan creado en docs/upstream-2026-06-24.md y ejecutado
- Changes applied (Bucket B): fix(breadcrumb) scroll horizontal (6963d82, cherry-pick limpio); fix(window) allow-destroy permission ya presente en el fork (414ee17, no action)
- Changes applied (Bucket C): font weight terminal (9fc0425, merge manual); shell selector (a770307, merge manual); language override por panel con selector UI en EditorOverlayBar (d77476e, merge manual + reimplementacion UI)
- Changes skipped (removed surface): Nix sources (564e145); version bump v0.8.2 (63099f5); npm dep bumps (db63229); AI autocomplete endpoints (a8b9481 -- superficie eliminada); quit-on-last-tab (7a812ca -- modelo de tabs eliminado en Phase 2)
- New features rejected: ninguna
```

**2. Actualizar la entrada anterior** (2026-06-22) para reemplazar cualquier nota de "SYNC AUN ABIERTO" si la hubiera (revisar el texto).
