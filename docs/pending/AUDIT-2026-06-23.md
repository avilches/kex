# Auditoria de pendientes - 2026-06-23

Barrido de todos los items de `PENDING.md` y `TODO.md` contra el codigo actual para detectar
los que ya estaban corregidos, los que cambiaron de alcance y los que siguen vigentes. Cada
veredicto se apoya en `archivo:linea` reales.

## Resumen

- Bugs eliminados (ya corregidos): BUG-20, BUG-27, BUG-30, BUG-34. (BUG-41 ya se habia cerrado antes por premisa invalida.)
- Mejoras eliminadas (ya hechas): M1.
- Tareas de TODO eliminadas (ya hechas): redimensionar ventana no persiste tamano.
- Reescritos por cambio de estado: BUG-39, BUG-40, F7, M7, y TODO 2/3/5/7.
- El resto sigue vigente.

## Revision 2 (2026-06-23, segundo barrido con verificacion directa)

Un segundo pase con el angulo "ya hecho / redundante / imposible por divergencia" reclasifico varios veredictos de la tabla original:

- Bugs ya HECHOS (no estaban en `PENDING.md`, solo en este informe como vigentes): BUG-05, BUG-11, BUG-28.
- Bug reducido a REDUNDANTE: BUG-14 (el diff principal ya propaga `truncated`; solo afecta git-history/commit-file).
- Features ya HECHAS (eliminadas de `PENDING.md` y sus ficheros de detalle): F8 (nav teclado), F9 (operaciones de fichero).
- TODO ya HECHO (eliminado de `TODO.md`): "Explorer lock a carpeta fija" (modelo `pinnedRoot` + UI "Set as Workspace Root" + `PinIcon` en header).
- TODO ya HECHO (eliminado en revision 1, confirmado): "Editor/preview guardar cwd" reclasificado a REDUNDANTE y borrado (la carpeta es derivable de `panel.path` y la sync ya ocurre via autofocus).
- M7: items 2, 3, 9, 10 ya hechos. Quedan 5 (parcial), 13, 14.

Las correcciones puntuales sobre las tablas de abajo estan marcadas con "[rev2]".

## Bugs

| ID | Veredicto | Nota |
| --- | --- | --- |
| BUG-02 | VIGENTE | `decode_text` (`git/process.rs:374`) no normaliza CRLF; worktree (CRLF) vs `git show` (LF). |
| BUG-03 | VIGENTE | `useDocument.ts:168` solo limpia el timer al desmontar, sin flush del buffer dirty. |
| BUG-04 | VIGENTE | `GitDiffPane.tsx:207-224` no incluye `themeExt` en deps; recrea EditorState al cambiar tema. |
| BUG-05 | YA-HECHO [rev2] | `diffSize.ts:16-23` `isDiffTooLarge` chequea bytes (256 KB) Y lineas (5000). |
| BUG-06 | VIGENTE | `diff_content` (`operations.rs:267`) no pasa `original_rel` al pathspec en rename staged. |
| BUG-07 | VIGENTE | `split_name_status_numstat` heuristica TAB; robusta para casos normales, fragil en extremos. |
| BUG-08 | VIGENTE | `shell_bg_kill` (`shell/mod.rs:266`) mata el proceso pero no lo quita del mapa (ring de 4 MiB). |
| BUG-11 | YA-HECHO [rev2] | `GitDiffPane.tsx:137,211,315-321` carga `truncated` en el state y muestra el badge "Truncated". |
| BUG-12 | VIGENTE | `panel_snapshot` (`operations.rs:119`) auto-autoriza el repo root ascendente sin acotar a cwd. |
| BUG-13 | VIGENTE | `decode_text` sniffa solo 8 KB; ficheros mixtos divergen de `git --numstat`. |
| BUG-14 | REDUNDANTE [rev2] | `diff_content` ya obtiene `truncated` de `diff_inner` (`operations.rs:276`); `git_show_text` sin propagar solo afecta git-history/commit-file. Alcance reducido. |
| BUG-15 | VIGENTE (mitigado) | El return de `useFileTree` sigue sin memoizar, pero `FileExplorer` ya lista props concretas en deps de `buildRows`. Reducir a: memoizar el return + estabilizar `options`. |
| BUG-17 | VIGENTE (parcial) | Infra de cancelacion ya existe en Rust (`grep.rs:242` generation counter); el frontend nunca la acciona. |
| BUG-18 | VIGENTE | `fetchChildren` (`useFileTree.ts:150`) sin token/secuencia; resolves fuera de orden pisan el mas reciente. |
| BUG-20 | YA-CORREGIDO (eliminado) | `pty/mod.rs:85` autoriza solo despues de spawn Ok. |
| BUG-27 | YA-CORREGIDO (eliminado) | `countDiffLines` (`GitDiffPane.tsx:90`) se usa solo en modo fallback, donde no hay numstat: workaround valido, no bug. |
| BUG-28 | YA-HECHO [rev2] | `parse_renamed` (`parser.rs:93`) ya descarta el entry con `if after.is_empty() || orig_path.is_empty()`. |
| BUG-30 | YA-CORREGIDO (eliminado) | Todos los catches de `useFileTree` y el autosave de `useDocument.ts:156` ya muestran `toast.error`. |
| BUG-34 | YA-CORREGIDO (eliminado) | `shellQuote.ts:3-8` ya usa comillas dobles (validas en cmd.exe y PowerShell). |
| BUG-38 | VIGENTE (parcial) | La navegacion con flechas en la busqueda ya esta (`SearchInline.tsx:175`). Sigue pendiente la busqueda real en paneles markdown/git-diff (lo que titula el bug). |
| BUG-39 | RECLASIFICADO | No es bug de codigo: los atajos (Cmd+P, Cmd+Shift+P, Cmd+Shift+F) funcionan. Es descubribilidad/UX: tratar como mejora de UI. |
| BUG-40 | DUDOSO | La construccion de pathspecs en stage/unstage all parece correcta; el sintoma probablemente cuelga de BUG-06 (renames sin `original_rel`). Reproducir con >100 ficheros antes de fix. |
| BUG-42 | VIGENTE (parcial) | `reload()` (`useDocument.ts:114`) captura el error generico pero no distingue ENOENT; no marca el tab como "borrado en disco" ni frena el autosave. Afinar a ese alcance. |
| BUG-43 | VIGENTE | El restore quita `--worktree` por diseno (`AGENT_SESSION_RESTORE.md`, `session_store.rs:125`): los worktrees se tratan como efimeros y si el cwd no existe el restore falla con "Directory not found". Documentar/decidir fallback a rama padre. |

## Features

| ID | Veredicto | Nota |
| --- | --- | --- |
| F1 | PENDIENTE | `GitDiffPane.tsx` usa `unifiedMergeView` (inline); no hay `MergeView` side-by-side. |
| F2 | PENDIENTE | `useSourceControlPanel.ts:480` solo stage por fichero; sin `git_apply_patch` ni parser de hunks. |
| F3 | PENDIENTE | Sin `nextChange`/`prevChange` ni contador de hunks en ningun sitio. |
| F5 | PENDIENTE | Sin pila de paneles cerrados ni atajo `tab.reopenClosed`. |
| F6 | PENDIENTE | `snapshot` existe en memoria de sesion (`useTerminalSession.ts:81`) pero no persiste a disco. |
| F7 | PARCIAL | Infra hecha: `tabBarStyle` en store + aplicado en `PaneTabBar.tsx`. Falta solo el selector en Settings. |
| F8 | YA-HECHO [rev2] | `FileExplorer.tsx:1007` `handleKeyDown` con nav por flechas; eliminado de PENDING. |
| F9 | YA-HECHO [rev2] | Operaciones en el menu de `TreeRow` + shortcuts `file.delete`/`file.paste`/`file.rename` registrados en `shortcuts.ts:202-211`; eliminado de PENDING. |
| F10 | PARCIAL | `leafHasForegroundProcess()` ya existe; falta el check + dialogo en el quit flow (`main.tsx:74-95`). |

## Mejoras

| ID | Veredicto | Nota |
| --- | --- | --- |
| M1 | YA-HECHA (eliminada) | Memoizacion del render tree completa: `SplitNodeView = memo(...)`, contextos DnD separados, estado efimero fuera del arbol. |
| M2 | PENDIENTE | `AgentNotificationsBridge` se importa estatico y registra listeners siempre (`App.tsx:16`). |
| M3 | PENDIENTE | No existe `git_diff_hunks` ni tipos `Hunk`/`DiffLine`. |
| M4 | PARCIAL | `fs_search` ya tiene generation counter; falta comando de cancelacion y accionarlo desde el frontend (y analogo en grep). |
| M5 | PARCIAL | Existe fallback por bytes; falta worker, virtualizacion del patch y guardia por lineas. |
| M6 | PENDIENTE | `shell_bg_kill` no purga el mapa; sin TTL/LRU ni consolidacion de roots. |
| M7 | PARCIAL [rev2] | ~11/15 hechos. Items 2/3/9/10 confirmados hechos (`iconResolver.ts:70`, `CommandPalette.tsx:393`, `ExplorerSearch.tsx:297`, split `/[\\/]/`). Quedan 5 (parcial), 13, 14. |
| M8 | PENDIENTE | `release.yml` no porta la cadena de AppImage (wayland strip, pin appimagetool, firma race-free). |
| M9 | PENDIENTE (trivial) | `file.rename` sigue con `defaultBindings: [{ key: "F2" }]`; cambiar a `{ shift: true, key: "F6" }`. |
| M10 | PARCIAL | `fs_search` ya async + spawn_blocking + generation; falta `build_parallel()`, tests de cancelacion y tuning de debounce/limite. |

## TODO.md

| Item | Veredicto | Nota |
| --- | --- | --- |
| Fecha original de sesion Claude | PENDIENTE | Sin `sessionCreatedAt` en `AgentSessionMeta`/payload. |
| Popup info todos los tabs | PARCIAL | El HoverCard ya es generico por kind; faltan metadata de git-history (rama) y browser (conexion). |
| Editor/preview guardar cwd | REDUNDANTE [rev2] | La carpeta es derivable de `panel.path` y la sync ya ocurre via autofocus; el browser no tiene ubicacion en disco. Eliminado de TODO. |
| Notas Markdown + Marcadores | PENDIENTE (producto nuevo) | Sin modulos `notes/` ni `bookmarks/`; vive en `NOTES_AND_BOOKMARKS_PLAN.md`. |
| Explorer lock a carpeta fija | YA-HECHO [rev2] | `explorerRootMode: "pinned"` + `pinnedRoot` + menu "Set as Workspace Root" (`TreeRow.tsx:320`) + `PinIcon` en header (`FileExplorer.tsx:130,1145`). Eliminado de TODO. |
| Drag desde explorer al SO | PENDIENTE | Sin `tauri-plugin-drag` en `Cargo.toml` ni comando `start_drag`. |
| Workspace label + barra superior | PARCIAL | `Workspace.title` ya existe; falta mostrarlo en la barra superior. |
| Redimensionar ventana no persiste | YA-HECHA (eliminada) | `window_state.rs:347` persiste geometria y `restore_window_geometry` la restaura. |
| Drag tab/fichero a otro workspace | PENDIENTE | DnD confinado al workspace activo (`WorkspaceDndProvider.tsx:149,300`). |
| Workspace estados configurables | PENDIENTE | Sin campo de estado en el modelo `Workspace`. |
| Updater menos agresivo | PENDIENTE | `UpdaterDialog.tsx` es modal; "Later" es temporal, sin "skip until next version". |
| Blocks toggle Cmd+U | PENDIENTE | El hint es decorativo; sin `terminal.toggleBlocks` ni re-eval de `KEX_BLOCKS` en precmd. |
| Explorer View File History | PENDIENTE | `git_log`/`log()` no aceptan path; panel `git-history` sin `filePath`. |
| Terminal opciones adicionales | PENDIENTE | bell/paste-protection/copy-on-select sin campo en `Preferences` ni handlers xterm. |
