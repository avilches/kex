# Agent session metadata popover -- Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mostrar metadata real de la sesion Claude (session ID, directorio, tiempo, restore) en un HoverCard sobre el tab de agente, con arquitectura extensible para futuros campos.

**Architecture:** Rust emite `kex:agent-session-meta` cuando llega `SessionStart`; el bridge TS escucha ese evento y guarda la metadata en un sub-objeto `meta` dentro de `AgentSession`; el tab reemplaza su `title` attribute por un `HoverCard` que renderiza la metadata.

**Tech Stack:** Rust/Tauri 2 (backend), React 19 + TypeScript + Zustand + shadcn HoverCard (frontend), vitest (tests).

## Global Constraints

- Sin em-dash en ningun texto generado.
- Sin emojis.
- Imports siempre `@/...` en el frontend, nunca relativos entre modulos.
- `pnpm` unicamente, nunca npm/npx/yarn.
- Checks de calidad: `pnpm lint`, `pnpm check-types`, `pnpm test`, `cd src-tauri && cargo clippy && cargo test --locked`.
- Commits frecuentes, sin "Generated by Claude Code" ni "Co-authored by".

---

### Task 1: Tipos y accion `setMeta` en el store

**Files:**
- Modify: `src/modules/agents/lib/types.ts`
- Modify: `src/modules/agents/store/agentStore.ts`
- Create: `src/modules/agents/store/agentStore.test.ts`

**Interfaces:**
- Produce: tipo `AgentSessionMeta`, campo `meta?: AgentSessionMeta` en `AgentSession`, accion `setMeta(panelId, meta)` en el store.
- Las tareas 3 y 4 consumen estos tipos.

- [ ] **Step 1: Anadir `AgentSessionMeta` y `meta` a `types.ts`**

En `src/modules/agents/lib/types.ts`, anadir despues de `AgentSession`:

```ts
export type AgentSessionMeta = {
  sessionId?: string;
  cwdLaunch?: string;
  // extension futura: contextPct?: number; model?: string;
};
```

Y anadir `meta?: AgentSessionMeta;` como ultimo campo de `AgentSession`:

```ts
export type AgentSession = {
  panelId: string;
  tabId: string;
  agent: string;
  status: AgentStatus;
  startedAt: number;
  lastActivityAt: number;
  attentionSince: number | null;
  restored: boolean;
  restoreError: boolean;
  restoreErrorReason?: string;
  meta?: AgentSessionMeta;
};
```

- [ ] **Step 2: Anadir `setMeta` al tipo y a la implementacion del store**

En `src/modules/agents/store/agentStore.ts`, anadir `setMeta` al tipo `AgentStoreState`:

```ts
setMeta: (panelId: string, meta: Partial<AgentSessionMeta>) => void;
```

Y anadir la implementacion en el objeto `create(...)`, despues de `clearRestored`:

```ts
setMeta: (panelId, meta) =>
  set((s) => {
    const prev = s.sessions[panelId];
    if (!prev) return s;
    return {
      sessions: {
        ...s.sessions,
        [panelId]: { ...prev, meta: { ...prev.meta, ...meta } },
      },
    };
  }),
```

Anadir `AgentSessionMeta` a la importacion de tipos al inicio del fichero:

```ts
import type {
  AgentNotification,
  AgentSession,
  AgentSessionMeta,
  AgentStatus,
  LocalAgentState,
} from "../lib/types";
```

- [ ] **Step 3: Escribir los tests de `setMeta`**

Crear `src/modules/agents/store/agentStore.test.ts`:

```ts
import { beforeEach, describe, expect, test } from "vitest";
import { useAgentStore } from "./agentStore";

describe("agentStore.setMeta", () => {
  beforeEach(() => {
    useAgentStore.setState({ sessions: {} });
  });

  test("no-op cuando el panelId no existe", () => {
    useAgentStore.getState().setMeta("unknown", { sessionId: "abc" });
    expect(useAgentStore.getState().sessions).toEqual({});
  });

  test("asigna sessionId a una sesion existente", () => {
    useAgentStore.getState().start("panel-1", "tab-1", "claude");
    useAgentStore.getState().setMeta("panel-1", { sessionId: "sess-abc" });
    expect(useAgentStore.getState().sessions["panel-1"].meta?.sessionId).toBe("sess-abc");
  });

  test("merge con meta existente sin pisar otros campos", () => {
    useAgentStore.getState().start("panel-1", "tab-1", "claude");
    useAgentStore.getState().setMeta("panel-1", { sessionId: "sess-abc" });
    useAgentStore.getState().setMeta("panel-1", { cwdLaunch: "/home/user/proyecto" });
    const meta = useAgentStore.getState().sessions["panel-1"].meta;
    expect(meta?.sessionId).toBe("sess-abc");
    expect(meta?.cwdLaunch).toBe("/home/user/proyecto");
  });

  test("sobreescribe un campo de meta en una llamada posterior", () => {
    useAgentStore.getState().start("panel-1", "tab-1", "claude");
    useAgentStore.getState().setMeta("panel-1", { sessionId: "viejo" });
    useAgentStore.getState().setMeta("panel-1", { sessionId: "nuevo" });
    expect(useAgentStore.getState().sessions["panel-1"].meta?.sessionId).toBe("nuevo");
  });

  test("setMeta no modifica otros campos de la sesion", () => {
    useAgentStore.getState().start("panel-1", "tab-1", "claude");
    const before = useAgentStore.getState().sessions["panel-1"];
    useAgentStore.getState().setMeta("panel-1", { sessionId: "abc" });
    const after = useAgentStore.getState().sessions["panel-1"];
    expect(after.agent).toBe(before.agent);
    expect(after.status).toBe(before.status);
    expect(after.startedAt).toBe(before.startedAt);
  });
});
```

- [ ] **Step 4: Ejecutar los tests y verificar que pasan**

```bash
pnpm test src/modules/agents/store/agentStore.test.ts
```

Esperado: 5 tests en verde.

- [ ] **Step 5: Verificar tipos**

```bash
pnpm check-types
```

Esperado: sin errores.

- [ ] **Step 6: Commit**

```bash
git add src/modules/agents/lib/types.ts src/modules/agents/store/agentStore.ts src/modules/agents/store/agentStore.test.ts
git commit -m "feat(agents): AgentSessionMeta type y accion setMeta en el store"
```

---

### Task 2: Evento Rust `kex:agent-session-meta`

**Files:**
- Modify: `src-tauri/src/modules/pty/session.rs`

**Interfaces:**
- Consume: `session_store::record_session` (ya existe), `app_reader.emit(...)` (patron existente en el mismo fichero).
- Produce: evento Tauri `"kex:agent-session-meta"` con payload `{ panelId: String, sessionId: String, cwdLaunch: String }` (camelCase via serde).

- [ ] **Step 1: Anadir constante y struct de payload en `session.rs`**

Anadir justo debajo de `const AGENT_EVENT: &str = "kex:agent-signal";` (linea 17):

```rust
const AGENT_SESSION_META_EVENT: &str = "kex:agent-session-meta";

#[derive(serde::Serialize)]
#[serde(rename_all = "camelCase")]
struct AgentSessionMetaPayload<'a> {
    panel_id: &'a str,
    session_id: &'a str,
    cwd_launch: &'a str,
}
```

- [ ] **Step 2: Emitir el evento tras `record_session`**

En `session.rs`, el bloque `SessionStart` actual (lineas 189-195) es:

```rust
if let Transition::SessionStart {
    ref panel_id, ref agent, ref session_id,
    ref transcript_path, ref cwd,
} = t {
    session_store::record_session(panel_id, agent, session_id, transcript_path, cwd);
    return;
}
```

Reemplazarlo por:

```rust
if let Transition::SessionStart {
    ref panel_id, ref agent, ref session_id,
    ref transcript_path, ref cwd,
} = t {
    session_store::record_session(panel_id, agent, session_id, transcript_path, cwd);
    let _ = app_reader.emit(AGENT_SESSION_META_EVENT, AgentSessionMetaPayload {
        panel_id,
        session_id,
        cwd_launch: cwd,
    });
    return;
}
```

- [ ] **Step 3: Verificar compilacion y clippy**

```bash
cd src-tauri && cargo clippy && cargo test --locked
```

Esperado: sin warnings ni errores. Los tests existentes de `session_store` siguen en verde.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/modules/pty/session.rs
git commit -m "feat(pty): emitir kex:agent-session-meta al frontend en SessionStart"
```

---

### Task 3: Listener en `AgentNotificationsBridge`

**Files:**
- Modify: `src/modules/agents/components/AgentNotificationsBridge.tsx`

**Interfaces:**
- Consume: evento `"kex:agent-session-meta"` (Task 2), `store.setMeta` (Task 1), tipo `AgentSessionMeta` de `@/modules/agents/lib/types`.
- Produce: rellena `session.meta` en el store cuando llega el evento.

- [ ] **Step 1: Anadir tipo del payload y segundo listener**

En `AgentNotificationsBridge.tsx`, anadir el tipo local del payload despues de las importaciones existentes:

```ts
type AgentSessionMetaPayload = {
  panelId: string;
  sessionId: string;
  cwdLaunch: string;
};
```

Anadir un segundo `useEffect` dentro de `AgentNotificationsBridge`, despues del `useEffect` existente:

```ts
useEffect(() => {
  let alive = true;
  let unlisten: (() => void) | undefined;
  listen<AgentSessionMetaPayload>("kex:agent-session-meta", (e) => {
    const { panelId, sessionId, cwdLaunch } = e.payload;
    useAgentStore.getState().setMeta(panelId, { sessionId, cwdLaunch });
  })
    .then((u) => {
      if (alive) unlisten = u;
      else u();
    })
    .catch((e) => console.error("[kex:agent] listen session-meta failed:", e));
  return () => {
    alive = false;
    unlisten?.();
  };
}, []);
```

- [ ] **Step 2: Verificar tipos y lint**

```bash
pnpm check-types && pnpm lint
```

Esperado: sin errores.

- [ ] **Step 3: Commit**

```bash
git add src/modules/agents/components/AgentNotificationsBridge.tsx
git commit -m "feat(agents): escuchar kex:agent-session-meta y guardar en session.meta"
```

---

### Task 4: HoverCard en el tab de agente

**Files:**
- Modify: `src/modules/workspaces/PaneTabBar.tsx`

**Interfaces:**
- Consume: `agentSession.meta?.sessionId`, `agentSession.meta?.cwdLaunch`, `agentSession.startedAt`, `agentSession.restored`, `agentSession.restoreError`, `panel.cwd` (todos ya disponibles en `DraggableTab`).
- `HoverCard`, `HoverCardContent`, `HoverCardTrigger` de `@/components/ui/hover-card` (ya instalado).

- [ ] **Step 1: Anadir helper `formatElapsed` y la importacion de HoverCard**

Al inicio de `PaneTabBar.tsx`, en el bloque de imports anadir:

```ts
import { HoverCard, HoverCardContent, HoverCardTrigger } from "@/components/ui/hover-card";
```

Debajo de las importaciones (antes de `type Props = ...`), anadir la funcion helper:

```ts
function formatElapsed(ms: number): string {
  const s = Math.floor(ms / 1000);
  if (s < 60) return `${s}s`;
  const m = Math.floor(s / 60);
  if (m < 60) return `${m}m ${s % 60}s`;
  const h = Math.floor(m / 60);
  return `${h}h ${m % 60}m`;
}
```

- [ ] **Step 2: Extraer componente `AgentHoverCardContent`**

Anadir el componente antes de `DraggableTab` en el fichero:

```tsx
function AgentHoverCardContent({
  agentSession,
  cwd,
}: {
  agentSession: NonNullable<ReturnType<typeof useAgentStore>["sessions"][string]>;
  cwd: string | undefined;
}) {
  const elapsed = formatElapsed(Date.now() - agentSession.startedAt);
  const sessionId = agentSession.meta?.sessionId;
  const directory = cwd ?? agentSession.meta?.cwdLaunch;

  return (
    <div className="space-y-2">
      <div className="flex items-center gap-1.5">
        <span className="text-[12px] font-medium text-foreground">{agentSession.agent}</span>
        {agentSession.status === "working" ? (
          <span className="size-[7px] shrink-0 animate-spin rounded-full border border-transparent border-t-foreground/70" />
        ) : (
          <span className="inline-block size-[6px] shrink-0 rounded-full bg-amber-400" />
        )}
      </div>
      <div className="grid grid-cols-[auto_1fr] gap-x-3 gap-y-0.5 text-[11px]">
        {sessionId && (
          <>
            <span className="text-muted-foreground">Session</span>
            <span className="truncate font-mono text-foreground">
              {sessionId.slice(0, 8)}&hellip;
            </span>
          </>
        )}
        {directory && (
          <>
            <span className="text-muted-foreground">Directory</span>
            <span className="truncate text-foreground">{directory}</span>
          </>
        )}
        <span className="text-muted-foreground">Started</span>
        <span className="text-foreground">hace {elapsed}</span>
        {agentSession.restored && (
          <>
            <span className="text-muted-foreground">Restored</span>
            <span className="text-foreground">si</span>
          </>
        )}
        {agentSession.restoreError && (
          <>
            <span className="text-muted-foreground">Error</span>
            <span className="truncate text-destructive">
              {agentSession.restoreErrorReason ?? "desconocido"}
            </span>
          </>
        )}
      </div>
    </div>
  );
}
```

Nota: `useAgentStore` ya esta importado en el fichero; para el tipo inline del parametro `agentSession` usa directamente el tipo `AgentSession` de `@/modules/agents/lib/types`:

```ts
import type { AgentSession } from "@/modules/agents/lib/types";
```

Y el componente queda:

```tsx
function AgentHoverCardContent({
  agentSession,
  cwd,
}: {
  agentSession: AgentSession;
  cwd: string | undefined;
}) {
```

- [ ] **Step 3: Envolver el tab en `HoverCard` y eliminar el atributo `title`**

En `DraggableTab`, el JSX actual devuelve:

```tsx
return (
  <Popover open={isRenaming} ...>
    <ContextMenu>
      ...
    </ContextMenu>
    <PopoverContent ...>...</PopoverContent>
  </Popover>
);
```

Pasarlo a:

```tsx
return (
  <HoverCard openDelay={700} closeDelay={100}>
    <Popover open={isRenaming} onOpenChange={(open) => { if (!open) handleSave(); }}>
      <ContextMenu>
        <ContextMenuTrigger asChild>
          <PopoverAnchor asChild>
            <HoverCardTrigger asChild>
              <div
                ref={setNodeRef}
                {...attributes}
                data-panel-id={panel.id}
                {/* ELIMINAR el atributo title que estaba aqui */}
                onClick={() => onActivate(panel.id)}
                onMouseDown={(e) => { if (e.button === 1) e.preventDefault(); }}
                onAuxClick={(e) => { if (e.button === 1) { e.stopPropagation(); onClose(panel.id); } }}
                {...listeners}
                className={cn(...)}
              >
                {/* contenido interno sin cambios */}
              </div>
            </HoverCardTrigger>
          </PopoverAnchor>
        </ContextMenuTrigger>
        <ContextMenuContent>...</ContextMenuContent>
      </ContextMenu>
      <PopoverContent ...>...</PopoverContent>
    </Popover>
    {hasAgent && !isRestoreError && (
      <HoverCardContent side="bottom" align="start" className="w-56 rounded-xl p-3">
        <AgentHoverCardContent agentSession={agentSession!} cwd={panel.kind === "terminal" ? panel.cwd : undefined} />
      </HoverCardContent>
    )}
  </HoverCard>
);
```

El atributo `title` del div exterior se elimina completamente. El `title` del span interno (el de `isRestoreError`) se mantiene -- es una alerta de error, no metadata de sesion.

- [ ] **Step 4: Verificar tipos, lint y tests**

```bash
pnpm check-types && pnpm lint && pnpm test
```

Esperado: sin errores. Los tests de `agentStore.test.ts` siguen en verde.

- [ ] **Step 5: Commit**

```bash
git add src/modules/workspaces/PaneTabBar.tsx
git commit -m "feat(tabs): HoverCard de metadata de sesion sobre el tab de agente"
```

---

## Self-review

**Cobertura del spec:**
- [x] Flujo Rust -> frontend via `kex:agent-session-meta`: Task 2 + 3
- [x] `AgentSessionMeta` y `meta?` en `AgentSession`: Task 1
- [x] Accion `setMeta` con merge: Task 1
- [x] Eliminar `title` attribute: Task 4
- [x] HoverCard con openDelay=700: Task 4
- [x] Campos: agent+status, sessionId (truncado), directory, elapsed, restored: Task 4
- [x] Campos ausentes si meta no ha llegado aun: `agentSession.meta?.sessionId` es opcional, no aparece si undefined
- [x] Extensibilidad: anadir campo a `AgentSessionMeta` + fila al componente

**Placeholder scan:** ningun TBD, ningun TODO, todo el codigo esta escrito.

**Consistencia de tipos:**
- `AgentSessionMeta` definido en Task 1, importado en Tasks 2 (no necesario), 3 (implicitamente via store), 4 (via importacion de tipos).
- `setMeta(panelId, meta)` definido en Task 1, usado en Task 3.
- `agentSession.meta?.sessionId` accedido en Task 4 coincide con la definicion de Task 1.
- `AgentHoverCardContent` recibe `AgentSession` (Task 1) y `cwd: string | undefined`.
