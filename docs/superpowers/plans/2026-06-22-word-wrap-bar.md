# Word Wrap Floating Bar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mover el control de word wrap de Settings a una barra flotante reutilizable en el editor, unificando el wrap en una unica preferencia global compartida por editor de codigo, editor de markdown y diff.

**Architecture:** Un boton presentacional `WrapToggleButton` lee/escribe la preferencia global `editorWordWrap`. Una barra flotante `EditorOverlayBar` (generaliza el actual `MarkdownViewToggle`) compone un toggle opcional `Rendered | Edit` mas el `WrapToggleButton`. Los tres consumidores (editor markdown, editor normal, diff) usan estas piezas; el diff deja de tener estado local de wrap.

**Tech Stack:** React 19 + TypeScript, CodeMirror 6 (`wrapCompartment`), Zustand (`usePreferencesStore`), tauri-plugin-store (persistencia), Tailwind v4, hugeicons (`TextWrapIcon`).

## Global Constraints

- Imports siempre `@/...`, nunca relativos entre modulos (excepto dentro del mismo modulo donde el codigo existente ya usa `./`).
- Sin em-dash, sin emojis, sin comentarios de relleno. Comentarios solo para el *why*, 1-2 lineas.
- Commits atomicos, mensaje en ingles, sin "Co-authored-by" ni "Generated by Claude Code".
- Verificacion por tarea: `pnpm lint`, `pnpm check-types`, `pnpm test` deben pasar.
- No hay infra de tests de componentes React; no se anaden tests nuevos (no es core subsystem). El invariante "wrap global unico" se valida con check-types + render manual.
- Trabajar en el worktree `.claude/worktrees/word-wrap-bar` (rama `word-wrap-bar`). Las dependencias ya estan instaladas.

---

### Task 1: `WrapToggleButton` (boton compartido conectado al global)

**Files:**
- Create: `src/modules/editor/WrapToggleButton.tsx`
- Modify: `src/modules/editor/index.ts` (anadir export)

**Interfaces:**
- Consumes: `usePreferencesStore` de `@/modules/settings/preferences`, `setEditorWordWrap` de `@/modules/settings/store`.
- Produces: `export function WrapToggleButton(props?: { className?: string }): JSX.Element`. Boton autonomo: lee `editorWordWrap` global y lo togglea al click. Reutilizable dentro de la barra flotante y en el header del diff.

- [ ] **Step 1: Crear el componente**

`src/modules/editor/WrapToggleButton.tsx`:

```tsx
import { cn } from "@/lib/utils";
import { usePreferencesStore } from "@/modules/settings/preferences";
import { setEditorWordWrap } from "@/modules/settings/store";
import { TextWrapIcon } from "@hugeicons/core-free-icons";
import { HugeiconsIcon } from "@hugeicons/react";

export function WrapToggleButton({ className }: { className?: string }) {
  const wordWrap = usePreferencesStore((s) => s.editorWordWrap);
  return (
    <button
      type="button"
      title={wordWrap ? "Disable word wrap" : "Enable word wrap"}
      onClick={() => void setEditorWordWrap(!wordWrap)}
      className={cn(
        "flex size-[22px] items-center justify-center rounded transition-colors",
        wordWrap
          ? "text-foreground"
          : "text-muted-foreground hover:text-foreground",
        className,
      )}
    >
      <HugeiconsIcon icon={TextWrapIcon} size={12} />
    </button>
  );
}
```

- [ ] **Step 2: Exportar desde el barrel del modulo editor**

En `src/modules/editor/index.ts`, anadir junto a los exports existentes:

```ts
export { WrapToggleButton } from "./WrapToggleButton";
```

- [ ] **Step 3: Verificar**

Run: `pnpm lint && pnpm check-types`
Expected: PASS, sin errores de import ni de tipos.

- [ ] **Step 4: Commit**

```bash
git add src/modules/editor/WrapToggleButton.tsx src/modules/editor/index.ts
git commit -m "feat(editor): add WrapToggleButton bound to global word wrap preference"
```

---

### Task 2: `EditorOverlayBar` (barra flotante reutilizable) y reconexion de consumidores

Generaliza el actual `MarkdownViewToggle` en una barra flotante que compone un toggle opcional `Rendered | Edit` mas el `WrapToggleButton`. El boton que antes decia `Raw` ahora dice `Edit` (solo cambia el label; el valor del callback sigue siendo `"raw"`).

**Files:**
- Create: `src/modules/editor/EditorOverlayBar.tsx`
- Delete: `src/modules/markdown/MarkdownViewToggle.tsx`
- Modify: `src/modules/markdown/index.ts` (quitar export de `MarkdownViewToggle`)
- Modify: `src/modules/editor/index.ts` (anadir export de `EditorOverlayBar`)
- Modify: `src/modules/markdown/MarkdownPreviewPane.tsx` (usar `EditorOverlayBar`)
- Modify: `src/modules/workspaces/PanelContent.tsx` (usar `EditorOverlayBar` en el caso editor)

**Interfaces:**
- Consumes: `WrapToggleButton` de `@/modules/editor` (Task 1), `cn` de `@/lib/utils`.
- Produces:
  ```ts
  type MarkdownViewMode = "rendered" | "raw";
  type EditorOverlayBarProps = {
    view?: {
      mode: MarkdownViewMode;
      onChange: (mode: MarkdownViewMode) => void;
      renderedDisabled?: boolean;
      renderedHint?: string;
    };
  };
  export function EditorOverlayBar(props: EditorOverlayBarProps): JSX.Element
  ```
  Si `view` esta presente renderiza el toggle `Rendered | Edit`; siempre renderiza el `WrapToggleButton`.

- [ ] **Step 1: Crear `EditorOverlayBar`**

`src/modules/editor/EditorOverlayBar.tsx`:

```tsx
import { cn } from "@/lib/utils";
import { WrapToggleButton } from "./WrapToggleButton";

type MarkdownViewMode = "rendered" | "raw";

type Props = {
  view?: {
    mode: MarkdownViewMode;
    onChange: (mode: MarkdownViewMode) => void;
    renderedDisabled?: boolean;
    renderedHint?: string;
  };
};

export function EditorOverlayBar({ view }: Props) {
  return (
    <div className="absolute right-3 top-3 z-10 inline-flex items-center gap-1 rounded-md border border-border/60 bg-card/85 p-0.5 text-[11px] shadow-sm backdrop-blur">
      {view && (
        <>
          <div className="inline-flex items-center gap-0.5">
            <button
              type="button"
              onClick={() => view.onChange("rendered")}
              disabled={view.renderedDisabled}
              title={view.renderedDisabled ? view.renderedHint : undefined}
              className={cn(
                "rounded px-2 py-0.5 transition-colors",
                view.mode === "rendered"
                  ? "bg-accent text-foreground"
                  : "text-muted-foreground hover:text-foreground",
                view.renderedDisabled &&
                  "cursor-not-allowed opacity-40 hover:text-muted-foreground",
              )}
            >
              Rendered
            </button>
            <button
              type="button"
              onClick={() => view.onChange("raw")}
              className={cn(
                "rounded px-2 py-0.5 transition-colors",
                view.mode === "raw"
                  ? "bg-accent text-foreground"
                  : "text-muted-foreground hover:text-foreground",
              )}
            >
              Edit
            </button>
          </div>
          <div className="h-4 w-px bg-border/60" />
        </>
      )}
      <WrapToggleButton />
    </div>
  );
}
```

- [ ] **Step 2: Exportar `EditorOverlayBar` desde el barrel editor**

En `src/modules/editor/index.ts`, anadir:

```ts
export { EditorOverlayBar } from "./EditorOverlayBar";
```

- [ ] **Step 3: Actualizar `MarkdownPreviewPane` para usar `EditorOverlayBar`**

En `src/modules/markdown/MarkdownPreviewPane.tsx`:

Reemplazar el import (linea 7):
```tsx
import { MarkdownViewToggle } from "./MarkdownViewToggle";
```
por:
```tsx
import { EditorOverlayBar } from "@/modules/editor";
```

Reemplazar el uso (linea 82):
```tsx
<MarkdownViewToggle mode="rendered" onChange={onSetView} />
```
por:
```tsx
<EditorOverlayBar view={{ mode: "rendered", onChange: onSetView }} />
```

- [ ] **Step 4: Actualizar `PanelContent` (caso editor)**

En `src/modules/workspaces/PanelContent.tsx`:

Reemplazar el import (linea 4):
```tsx
import { MarkdownViewToggle } from "@/modules/markdown/MarkdownViewToggle";
```
por:
```tsx
import { EditorOverlayBar } from "@/modules/editor";
```

Reemplazar el bloque del caso `editor` (lineas 109-116) por:
```tsx
            {isMarkdownPath(panel.path) ? (
              <EditorOverlayBar
                view={{
                  mode: "raw",
                  onChange: (mode) => callbacks.onSetMarkdownView?.(panel.id, mode),
                  renderedDisabled: panel.dirty,
                  renderedHint: "Save to preview",
                }}
              />
            ) : (
              <EditorOverlayBar />
            )}
```

Nota: el editor normal (no `.md`) ahora muestra la barra flotante con solo el boton de wrap. El contenedor `div.relative.h-full.w-full` (linea 108) ya posiciona el `absolute`.

- [ ] **Step 5: Eliminar `MarkdownViewToggle` y su export**

Borrar el fichero:
```bash
git rm src/modules/markdown/MarkdownViewToggle.tsx
```

En `src/modules/markdown/index.ts`, quitar la linea:
```ts
export { MarkdownViewToggle } from "./MarkdownViewToggle";
```
(dejar el resto del barrel intacto).

- [ ] **Step 6: Verificar que no quedan referencias**

Run: `grep -rn "MarkdownViewToggle" src`
Expected: sin resultados.

Run: `pnpm lint && pnpm check-types`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(editor): floating overlay bar with optional Rendered/Edit toggle and wrap button"
```

---

### Task 3: Unificar el wrap del diff a la preferencia global

`GitDiffPane` deja de tener estado local de wrap y pasa a leer/escribir la preferencia global via `WrapToggleButton`, reconfigurando su `wrapCompartment` de forma reactiva.

**Files:**
- Modify: `src/modules/editor/GitDiffPane.tsx`

**Interfaces:**
- Consumes: `WrapToggleButton` de `@/modules/editor` (Task 1), `usePreferencesStore` de `@/modules/settings/preferences`.

- [ ] **Step 1: Cambiar imports**

En `src/modules/editor/GitDiffPane.tsx`:

Quitar las dos lineas de import del icono (lineas 4-5), que dejan de usarse:
```tsx
import { TextWrapIcon } from "@hugeicons/core-free-icons";
import { HugeiconsIcon } from "@hugeicons/react";
```

Anadir (junto a los imports de `./lib/...`, respetando el orden existente):
```tsx
import { usePreferencesStore } from "@/modules/settings/preferences";
import { WrapToggleButton } from "./WrapToggleButton";
```

- [ ] **Step 2: Sustituir el estado local por la preferencia global**

Reemplazar la linea 135:
```tsx
  const [wordWrap, setWordWrap] = useState(false);
```
por:
```tsx
  const editorWordWrap = usePreferencesStore((s) => s.editorWordWrap);
```

- [ ] **Step 3: Inicializar el compartment desde el global**

Reemplazar la linea 211 dentro del `useMemo` de `extensions`:
```tsx
      wrapCompartment.of([]),
```
por:
```tsx
      wrapCompartment.of(
        usePreferencesStore.getState().editorWordWrap
          ? EditorView.lineWrapping
          : [],
      ),
```

- [ ] **Step 4: Reconfigurar de forma reactiva al cambiar el global**

Reemplazar el effect de las lineas 226-234:
```tsx
  useEffect(() => {
    const view = cmRef.current?.view;
    if (!view) return;
    view.dispatch({
      effects: wrapCompartment.reconfigure(
        wordWrap ? EditorView.lineWrapping : [],
      ),
    });
  }, [wordWrap]);
```
por:
```tsx
  useEffect(() => {
    const view = cmRef.current?.view;
    if (!view) return;
    view.dispatch({
      effects: wrapCompartment.reconfigure(
        editorWordWrap ? EditorView.lineWrapping : [],
      ),
    });
  }, [editorWordWrap]);
```

- [ ] **Step 5: Sustituir el boton inline por `WrapToggleButton`**

Reemplazar el bloque del boton (lineas 302-309):
```tsx
          <button
            type="button"
            title={wordWrap ? "Disable word wrap" : "Enable word wrap"}
            onClick={() => setWordWrap((v) => !v)}
            className={`flex size-[22px] items-center justify-center rounded transition-colors ${wordWrap ? "text-foreground" : "text-muted-foreground hover:text-foreground"}`}
          >
            <HugeiconsIcon icon={TextWrapIcon} size={12} />
          </button>
```
por:
```tsx
          <WrapToggleButton />
```

- [ ] **Step 6: Verificar**

Run: `grep -n "wordWrap\|setWordWrap\|TextWrapIcon\|HugeiconsIcon" src/modules/editor/GitDiffPane.tsx`
Expected: solo aparece `editorWordWrap` (la lectura del store y el effect). Sin `setWordWrap`, sin `TextWrapIcon`, sin `HugeiconsIcon`.

Run: `pnpm lint && pnpm check-types && pnpm test`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/modules/editor/GitDiffPane.tsx
git commit -m "refactor(editor): bind diff word wrap to global preference"
```

---

### Task 4: Quitar el switch de Word wrap de Settings

Elimina el control de Settings. La preferencia, su default, persistencia y `setEditorWordWrap` permanecen intactos en el store.

**Files:**
- Modify: `src/settings/sections/GeneralSection.tsx`

- [ ] **Step 1: Quitar el import del setter**

En `src/settings/sections/GeneralSection.tsx`, quitar de la lista de imports de `@/modules/settings/store` (linea 21) la entrada:
```tsx
  setEditorWordWrap,
```

- [ ] **Step 2: Quitar la lectura del store**

Quitar la linea 38:
```tsx
  const editorWordWrap = usePreferencesStore((s) => s.editorWordWrap);
```

- [ ] **Step 3: Quitar el `SettingRow`**

Quitar el bloque completo (lineas 116-124):
```tsx
        <SettingRow
          title="Word wrap"
          description="Wrap long lines instead of scrolling horizontally."
        >
          <Switch
            checked={editorWordWrap}
            onCheckedChange={(v) => void setEditorWordWrap(v)}
          />
        </SettingRow>
```
Dejar "Vim mode" y "Auto save" en la seccion Editor.

- [ ] **Step 4: Verificar**

Run: `grep -n "editorWordWrap\|setEditorWordWrap\|Word wrap" src/settings/sections/GeneralSection.tsx`
Expected: sin resultados.

Run: `pnpm lint && pnpm check-types`
Expected: PASS, sin warnings de import o variable sin usar.

- [ ] **Step 5: Commit**

```bash
git add src/settings/sections/GeneralSection.tsx
git commit -m "feat(settings): remove word wrap switch from general settings"
```

---

### Task 5: Documentacion viva

Reflejar que el control de wrap se reubica y se unifica como preferencia global.

**Files:**
- Modify: `docs/FORK.md`
- Modify: `docs/ARCHITECTURE.md` (solo si hay una seccion de editor/preferencias donde encaje; si no, omitir)

- [ ] **Step 1: Anotar en `docs/FORK.md`**

Anadir una entrada factual (en la fase/seccion adecuada del fork) indicando: el word wrap del editor deja de ser un switch en Settings y pasa a un boton flotante en la barra del editor (`EditorOverlayBar` + `WrapToggleButton`), unificado como preferencia global unica compartida por editor de codigo, editor de markdown y diff. El toggle de markdown pasa de `Rendered | Raw` a `Rendered | Edit`.

- [ ] **Step 2: Revisar `docs/ARCHITECTURE.md`**

Si existe una mencion al modulo editor/markdown o a la barra de vista de markdown, actualizarla para reflejar `EditorOverlayBar`. Si no hay un punto natural, no forzar el cambio.

- [ ] **Step 3: Verificar**

Run: `grep -rn "MarkdownViewToggle" docs`
Expected: sin resultados (o actualizado a `EditorOverlayBar`).

- [ ] **Step 4: Commit**

```bash
git add docs/FORK.md docs/ARCHITECTURE.md
git commit -m "docs: record word wrap move to floating editor bar"
```

---

## Self-Review

**Spec coverage:**
- Quitar switch de Settings, preferencia persiste -> Task 4 (switch fuera; store intacto).
- Barra flotante reutilizable estilo `MarkdownViewToggle` -> Task 2 (`EditorOverlayBar`).
- Markdown: `Raw` -> `Edit`, anadir wrap -> Task 2 (label `Edit`, `WrapToggleButton` en la barra).
- Editor normal usa la misma barra sin `Rendered | Edit` -> Task 2 (`<EditorOverlayBar />` sin `view`).
- Wrap global unico, diff incluido -> Task 1 (boton global) + Task 3 (diff al global).
- Wrap siempre visible en markdown (incluido modo Rendered) -> Task 2 (`WrapToggleButton` siempre presente).

**Type consistency:** `MarkdownViewMode = "rendered" | "raw"` se mantiene en `EditorOverlayBar`, `MarkdownPreviewPane.onSetView`, `PanelContent.onSetMarkdownView` y el callback del store. El label visible `Edit` no cambia el valor `"raw"`. `WrapToggleButton` no recibe estado por props (lee el store), evita desincronizacion.

**Placeholder scan:** sin TBD/TODO; todo el codigo a insertar esta escrito.
