# Focus on Explorer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Anadir una accion "Focus on Explorer" al menu contextual de cualquier tab con fichero que revele y seleccione ese fichero en el Explorer, abriendo las carpetas padre y, si hace falta, cambiando el modo a File System y su root.

**Architecture:** Nucleo de decision en funciones puras testeables (`explorerRoot.ts` y un nuevo `panelPath.ts`). El shell imperativo es delgado: `App.tsx` decide modo/root y dispara una peticion reactiva `revealRequest`; un effect "driver" en `FileExplorer` reacciona a la recarga asincrona del arbol y revela el fichero. El item de menu vive en `PaneTabBar` y el callback viaja por el objeto `callbacks: PanelCallbacks` ya existente.

**Tech Stack:** React 19 + TypeScript, Vitest, hugeicons (`CrosshairIcon`).

## Global Constraints

- No em-dash en codigo, comentarios, commits ni docs.
- No emojis en ningun sitio.
- Imports siempre `@/...`, nunca relativos entre modulos (dentro de un mismo modulo los relativos `./...` existentes son validos).
- Comentarios: por defecto ninguno; si hace falta, 1-2 lineas sobre el *por que*.
- pnpm only. Checks: `pnpm lint`, `pnpm check-types`, `pnpm test`.
- Commits atomicos, mensaje en ingles, sin "Co-authored-by" ni "Generated by".
- Paths canonicos en frontend: forward-slash. Normalizar separadores con `.split(/[\\/]/)` o `.replace(/\\/g, "/")` en limites.

---

## File Structure

- `src/modules/workspaces/lib/explorerRoot.ts` (modify) — anade `isUnder`, `commonAncestor`, `ancestorsToExpand`, `resolveFocusTarget`.
- `src/modules/workspaces/lib/explorerRoot.test.ts` (modify) — tests de las 4 funciones nuevas.
- `src/modules/workspaces/lib/panelPath.ts` (create) — `panelFilePath(panel)`: path absoluto de fichero de un panel, o null.
- `src/modules/workspaces/lib/panelPath.test.ts` (create) — tests de `panelFilePath`.
- `src/modules/explorer/FileExplorer.tsx` (modify) — tipo `RevealRequest`, prop `revealRequest`, effect driver de revelado.
- `src/modules/explorer/index.ts` (modify) — exporta `RevealRequest`.
- `src/app/components/RightPanel.tsx` (modify) — prop `revealRequest` pass-through a `FileExplorer`.
- `src/modules/workspaces/PanelContent.tsx` (modify) — anade `onFocusOnExplorer` a `PanelCallbacks`.
- `src/app/App.tsx` (modify) — estado `revealRequest`, handler `handleFocusOnExplorer`, entrada en `panelCallbacks`, prop a las dos instancias de `RightPanel`.
- `src/modules/workspaces/PaneTabBar.tsx` (modify) — item de menu + prop drilling `onFocusOnExplorer`.
- `src/modules/workspaces/PaneView.tsx` (modify) — pasa `onFocusOnExplorer={callbacks.onFocusOnExplorer}` a `PaneTabBar`.

---

## Task 1: Pure core en explorerRoot.ts

**Files:**
- Modify: `src/modules/workspaces/lib/explorerRoot.ts`
- Test: `src/modules/workspaces/lib/explorerRoot.test.ts`

**Interfaces:**
- Consumes: `pathDirname` de `@/lib/pathUtils` (ya importado en el fichero).
- Produces:
  - `isUnder(path: string, root: string): boolean`
  - `commonAncestor(a: string, b: string): string | null`
  - `ancestorsToExpand(root: string, file: string): string[]`
  - `resolveFocusTarget(input: { file: string; mode: ExplorerRootMode; currentRoot: string | null; fsRoot: string | null; home: string | null }): { nextMode: ExplorerRootMode; nextFsRoot: string } | null`

- [ ] **Step 1: Escribir los tests que fallan**

Anadir al final de `src/modules/workspaces/lib/explorerRoot.test.ts`. Actualizar tambien el import de la cabecera para incluir los simbolos nuevos:

```ts
import {
  ancestorsToExpand,
  commonAncestor,
  isFilesystemRoot,
  isUnder,
  parentRoot,
  resolveExplorerRoot,
  resolveFocusTarget,
} from "./explorerRoot";
```

```ts
describe("isUnder", () => {
  it("treats equal paths as under", () => {
    expect(isUnder("/a/b", "/a/b")).toBe(true);
  });
  it("detects a descendant", () => {
    expect(isUnder("/a/b/c", "/a/b")).toBe(true);
  });
  it("rejects a sibling prefix", () => {
    expect(isUnder("/a/bc", "/a/b")).toBe(false);
  });
  it("rejects an unrelated path", () => {
    expect(isUnder("/x/y", "/a/b")).toBe(false);
  });
});

describe("commonAncestor", () => {
  it("returns the deepest shared directory", () => {
    expect(commonAncestor("/a/b/c", "/a/b/d/e")).toBe("/a/b");
  });
  it("returns the shallower path when one contains the other", () => {
    expect(commonAncestor("/a", "/a/b/c")).toBe("/a");
  });
  it("falls back to unix root when only root is shared", () => {
    expect(commonAncestor("/x/y", "/a/b")).toBe("/");
  });
  it("returns the drive root when only the drive is shared", () => {
    expect(commonAncestor("C:/a/b", "C:/c")).toBe("C:/a".slice(0, 2) + "/");
  });
  it("returns null across different drives", () => {
    expect(commonAncestor("C:/a", "D:/b")).toBeNull();
  });
  it("normalizes backslashes", () => {
    expect(commonAncestor("C:\\a\\b", "C:\\a\\c")).toBe("C:/a");
  });
});

describe("ancestorsToExpand", () => {
  it("lists dirs from the child of root down to the file's parent", () => {
    expect(ancestorsToExpand("/a", "/a/b/c/file.ts")).toEqual(["/a/b", "/a/b/c"]);
  });
  it("returns empty when the file sits directly under root", () => {
    expect(ancestorsToExpand("/a/b", "/a/b/file.ts")).toEqual([]);
  });
  it("returns empty when the file is not under root", () => {
    expect(ancestorsToExpand("/a/b", "/x/y/file.ts")).toEqual([]);
  });
});

describe("resolveFocusTarget", () => {
  const file = "/home/u/proj/src/deep/file.ts";

  it("returns null when the current view already contains the file", () => {
    expect(
      resolveFocusTarget({
        file,
        mode: "git",
        currentRoot: "/home/u/proj",
        fsRoot: null,
        home: "/home/u",
      }),
    ).toBeNull();
  });

  it("switches to filesystem using the common ancestor when the view does not contain the file", () => {
    expect(
      resolveFocusTarget({
        file,
        mode: "git",
        currentRoot: "/other/repo",
        fsRoot: "/home/u/notes",
        home: "/home/u",
      }),
    ).toEqual({ nextMode: "filesystem", nextFsRoot: "/home/u" });
  });

  it("keeps the fsRoot when it already contains the file", () => {
    expect(
      resolveFocusTarget({
        file,
        mode: "terminal",
        currentRoot: "/tmp",
        fsRoot: "/home/u/proj",
        home: "/home/u",
      }),
    ).toEqual({ nextMode: "filesystem", nextFsRoot: "/home/u/proj" });
  });

  it("uses home as the reference when fsRoot is null", () => {
    expect(
      resolveFocusTarget({
        file,
        mode: "pinned",
        currentRoot: null,
        fsRoot: null,
        home: "/home/u",
      }),
    ).toEqual({ nextMode: "filesystem", nextFsRoot: "/home/u" });
  });

  it("falls back to the file's parent dir when there is no common ancestor", () => {
    expect(
      resolveFocusTarget({
        file: "C:/work/a/file.ts",
        mode: "terminal",
        currentRoot: "D:/x",
        fsRoot: "D:/x",
        home: "D:/x",
      }),
    ).toEqual({ nextMode: "filesystem", nextFsRoot: "C:/work/a" });
  });
});
```

- [ ] **Step 2: Ejecutar los tests para verlos fallar**

Run: `pnpm test -- src/modules/workspaces/lib/explorerRoot.test.ts`
Expected: FAIL ("isUnder is not a function" / "commonAncestor is not exported", etc.).

- [ ] **Step 3: Implementar las funciones**

Anadir al final de `src/modules/workspaces/lib/explorerRoot.ts`:

```ts
function normalizeSep(path: string): string {
  return path.replace(/\\/g, "/");
}

export function isUnder(path: string, root: string): boolean {
  const p = normalizeSep(path);
  const r = normalizeSep(root);
  return p === r || p.startsWith(r.endsWith("/") ? r : `${r}/`);
}

export function commonAncestor(a: string, b: string): string | null {
  const sa = normalizeSep(a).split("/");
  const sb = normalizeSep(b).split("/");
  const common: string[] = [];
  for (let i = 0; i < sa.length && i < sb.length; i++) {
    if (sa[i] !== sb[i]) break;
    common.push(sa[i]);
  }
  if (common.length === 0) return null;
  if (common.length === 1) {
    if (common[0] === "") return "/";
    if (BARE_DRIVE.test(common[0])) return `${common[0]}/`;
    return null;
  }
  return common.join("/");
}

export function ancestorsToExpand(root: string, file: string): string[] {
  const r = normalizeSep(root);
  let dir = pathDirname(normalizeSep(file));
  if (!isUnder(dir, r)) return [];
  const out: string[] = [];
  while (dir !== r && isUnder(dir, r)) {
    out.unshift(dir);
    const parent = pathDirname(dir);
    if (parent === dir) break;
    dir = parent;
  }
  return out;
}

export function resolveFocusTarget(input: {
  file: string;
  mode: ExplorerRootMode;
  currentRoot: string | null;
  fsRoot: string | null;
  home: string | null;
}): { nextMode: ExplorerRootMode; nextFsRoot: string } | null {
  const file = normalizeSep(input.file);
  if (input.currentRoot && isUnder(file, input.currentRoot)) return null;
  const fsRef = input.fsRoot ?? input.home;
  const ca = fsRef ? commonAncestor(fsRef, file) : null;
  const nextFsRoot = ca ?? pathDirname(file);
  return { nextMode: "filesystem", nextFsRoot };
}
```

- [ ] **Step 4: Ejecutar los tests para verlos pasar**

Run: `pnpm test -- src/modules/workspaces/lib/explorerRoot.test.ts`
Expected: PASS (todos los describe, incluidos los previos de `resolveExplorerRoot`).

- [ ] **Step 5: Commit**

```bash
git add src/modules/workspaces/lib/explorerRoot.ts src/modules/workspaces/lib/explorerRoot.test.ts
git commit -m "feat(explorer): add focus-target resolution helpers"
```

---

## Task 2: Helper panelFilePath

**Files:**
- Create: `src/modules/workspaces/lib/panelPath.ts`
- Test: `src/modules/workspaces/lib/panelPath.test.ts`

**Interfaces:**
- Consumes: `Panel` de `./types`.
- Produces: `panelFilePath(panel: Panel): string | null` — path absoluto forward-slash del fichero del panel, o null si el panel no representa un fichero.

- [ ] **Step 1: Escribir los tests que fallan**

Crear `src/modules/workspaces/lib/panelPath.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { panelFilePath } from "./panelPath";
import type { Panel } from "./types";

describe("panelFilePath", () => {
  it("returns the editor path", () => {
    const p: Panel = { id: "1", kind: "editor", path: "/a/b.ts", dirty: false, preview: false };
    expect(panelFilePath(p)).toBe("/a/b.ts");
  });

  it("returns the markdown path", () => {
    const p: Panel = { id: "1", kind: "markdown", path: "/a/README.md" };
    expect(panelFilePath(p)).toBe("/a/README.md");
  });

  it("joins repoRoot for a relative git-diff path", () => {
    const p: Panel = {
      id: "1", kind: "git-diff", path: "src/x.ts", repoRoot: "/repo",
      mode: "+", originalPath: null,
    };
    expect(panelFilePath(p)).toBe("/repo/src/x.ts");
  });

  it("keeps an absolute git-diff path", () => {
    const p: Panel = {
      id: "1", kind: "git-diff", path: "/repo/src/x.ts", repoRoot: "/repo",
      mode: "+", originalPath: null,
    };
    expect(panelFilePath(p)).toBe("/repo/src/x.ts");
  });

  it("joins repoRoot for a git-commit-file path", () => {
    const p: Panel = {
      id: "1", kind: "git-commit-file", path: "src/x.ts", repoRoot: "/repo",
      sha: "abc", originalPath: null,
    };
    expect(panelFilePath(p)).toBe("/repo/src/x.ts");
  });

  it("returns null for terminal panels", () => {
    const p: Panel = { id: "1", kind: "terminal" };
    expect(panelFilePath(p)).toBeNull();
  });

  it("returns null for browser and git-history panels", () => {
    expect(panelFilePath({ id: "1", kind: "browser", url: "x" })).toBeNull();
    expect(panelFilePath({ id: "1", kind: "git-history", repoRoot: "/r" })).toBeNull();
  });
});
```

- [ ] **Step 2: Ejecutar el test para verlo fallar**

Run: `pnpm test -- src/modules/workspaces/lib/panelPath.test.ts`
Expected: FAIL ("Cannot find module './panelPath'").

- [ ] **Step 3: Implementar el helper**

Crear `src/modules/workspaces/lib/panelPath.ts`:

```ts
import type { Panel } from "./types";

const ABSOLUTE = /^([A-Za-z]:|\/|\\)/;

function join(repoRoot: string, path: string): string {
  return `${repoRoot.replace(/[\\/]+$/, "")}/${path.replace(/^[\\/]+/, "")}`.replace(
    /\\/g,
    "/",
  );
}

export function panelFilePath(panel: Panel): string | null {
  switch (panel.kind) {
    case "editor":
    case "markdown":
      return panel.path.replace(/\\/g, "/");
    case "git-diff":
      return ABSOLUTE.test(panel.path)
        ? panel.path.replace(/\\/g, "/")
        : join(panel.repoRoot, panel.path);
    case "git-commit-file":
      return join(panel.repoRoot, panel.path);
    default:
      return null;
  }
}
```

- [ ] **Step 4: Ejecutar el test para verlo pasar**

Run: `pnpm test -- src/modules/workspaces/lib/panelPath.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/modules/workspaces/lib/panelPath.ts src/modules/workspaces/lib/panelPath.test.ts
git commit -m "feat(workspaces): add panelFilePath helper"
```

---

## Task 3: RevealRequest y driver de revelado en FileExplorer

**Files:**
- Modify: `src/modules/explorer/FileExplorer.tsx`
- Modify: `src/modules/explorer/index.ts`
- Modify: `src/app/components/RightPanel.tsx`

**Interfaces:**
- Consumes: `isUnder`, `ancestorsToExpand` de `@/modules/workspaces/lib/explorerRoot`; `tree.expand`, `tree.expanded`, `tree.nodes`, `setSelectedPath`, `scrollEntryIntoView`, `entryIndexByPath` (ya existentes en `FileExplorer`).
- Produces: `type RevealRequest = { path: string; nonce: number }` (export); prop `revealRequest?: RevealRequest | null` en `FileExplorer` y en `RightPanelProps`.

- [ ] **Step 1: Anadir el tipo y la prop a FileExplorer**

En `src/modules/explorer/FileExplorer.tsx`, anadir el import (junto a los de `@/modules/workspaces/lib/explorerRoot`, linea ~62):

```ts
import {
  ancestorsToExpand,
  isUnder,
  type ExplorerRootMode,
} from "@/modules/workspaces/lib/explorerRoot";
```

(Sustituye el import existente `import type { ExplorerRootMode } from "@/modules/workspaces/lib/explorerRoot";` por el de arriba.)

Anadir el tipo exportado encima de `export type FileExplorerHandle` (linea ~64):

```ts
export type RevealRequest = { path: string; nonce: number };
```

Anadir la prop al `type Props` (tras `activeFilePath?: string | null;`, linea ~87):

```ts
  revealRequest?: RevealRequest | null;
```

Anadir `revealRequest` a la desestructuracion de props del componente. El componente usa `forwardRef<FileExplorerHandle, Props>(function FileExplorer(props, ref)` (linea ~298) y luego lee `props.X`/desestructura. Localizar la desestructuracion de props (las variables como `rootPath`, `activeFilePath` ya estan en scope como `props.rootPath` o desestructuradas). Confirmar el estilo leyendo lineas 298-330 y anadir `revealRequest` siguiendo el mismo estilo que `activeFilePath`.

- [ ] **Step 2: Anadir el effect driver**

En `src/modules/explorer/FileExplorer.tsx`, justo despues del effect de `activeFilePath` (que termina en la linea ~500 con `}, [activeFilePath, entryIndexByPath, scrollEntryIntoView]);`), anadir:

```ts
    const revealConsumedRef = useRef<number | null>(null);
    useEffect(() => {
      if (!revealRequest) return;
      if (revealConsumedRef.current === revealRequest.nonce) return;
      const file = revealRequest.path;
      if (!rootPath || !isUnder(file, rootPath)) return;
      if (tree.nodes[rootPath]?.status !== "loaded") return;
      let pending = false;
      for (const dir of ancestorsToExpand(rootPath, file)) {
        if (!tree.expanded.has(dir)) {
          tree.expand(dir);
          pending = true;
        } else if (tree.nodes[dir]?.status !== "loaded") {
          pending = true;
        }
      }
      if (pending) return;
      if (!entryIndexByPath.has(file)) return;
      revealConsumedRef.current = revealRequest.nonce;
      setSelectedPath(file);
      requestAnimationFrame(() => scrollEntryIntoView(file));
    }, [
      revealRequest,
      rootPath,
      tree.nodes,
      tree.expanded,
      tree.expand,
      entryIndexByPath,
      scrollEntryIntoView,
    ]);
```

Nota: este effect NO llama a `focus()`; el foco se queda en el editor por diseno.

- [ ] **Step 3: Exportar RevealRequest desde el barrel**

En `src/modules/explorer/index.ts`, ampliar la primera linea:

```ts
export { FileExplorer, type FileExplorerHandle, type RevealRequest } from "./FileExplorer";
```

- [ ] **Step 4: Pasar la prop por RightPanel**

En `src/app/components/RightPanel.tsx`:

Import del tipo (junto al import de `FileExplorer`, linea 2):

```ts
import { FileExplorer, type FileExplorerHandle, type RevealRequest } from "@/modules/explorer";
```

Anadir a `RightPanelProps` (tras `activeFilePath?: string | null;`, linea ~48):

```ts
  revealRequest?: RevealRequest | null;
```

Pasar a `<FileExplorer>` (tras `activeFilePath={props.activeFilePath}`, linea ~134):

```tsx
              revealRequest={props.revealRequest}
```

- [ ] **Step 5: Verificar tipos y lint**

Run: `pnpm check-types && pnpm lint`
Expected: PASS (sin errores). En este punto `revealRequest` aun no se pasa desde `App`, lo cual es valido porque la prop es opcional.

- [ ] **Step 6: Commit**

```bash
git add src/modules/explorer/FileExplorer.tsx src/modules/explorer/index.ts src/app/components/RightPanel.tsx
git commit -m "feat(explorer): add revealRequest driver to FileExplorer"
```

---

## Task 4: Wiring en App.tsx (estado, handler, callback, prop)

**Files:**
- Modify: `src/modules/workspaces/PanelContent.tsx`
- Modify: `src/app/App.tsx`

**Interfaces:**
- Consumes: `resolveFocusTarget` de `@/modules/workspaces/lib/explorerRoot`; `RevealRequest` de `@/modules/explorer`; `setExplorerRootMode`, `setFsRoot` (ya desestructurados de `useWorkspaces`); `setRightPanelOpen`, `setRightPanelActiveTab` (ya importados).
- Produces: `panelCallbacks.onFocusOnExplorer: (filePath: string) => void`; prop `revealRequest` en ambas instancias de `RightPanel`.

- [ ] **Step 1: Anadir onFocusOnExplorer a PanelCallbacks**

En `src/modules/workspaces/PanelContent.tsx`, dentro de `export type PanelCallbacks = {` (tras `onRenameFile?: ...;`, antes del cierre `};`):

```ts
  // Reveal an editor/markdown/git file in the explorer tree
  onFocusOnExplorer?: (filePath: string) => void;
```

- [ ] **Step 2: Importar resolveFocusTarget y RevealRequest en App**

En `src/app/App.tsx`, dentro del import de `@/modules/workspaces/lib/explorerRoot` (linea ~99, donde ya se importan `resolveExplorerRoot`, `isFilesystemRoot`, `parentRoot`), anadir `resolveFocusTarget`. Y donde se importa de `@/modules/explorer`, anadir `type RevealRequest` (si no hay import de ese modulo, anadir uno nuevo):

```ts
import type { RevealRequest } from "@/modules/explorer";
```

- [ ] **Step 3: Anadir el estado revealRequest**

En `src/app/App.tsx`, junto al resto de estado de UI del explorer (cerca de la linea ~309 donde esta `const rightPanelRef = useRef<RightPanelHandle>(null);`):

```ts
  const [revealRequest, setRevealRequest] = useState<RevealRequest | null>(null);
```

- [ ] **Step 4: Anadir el handler handleFocusOnExplorer**

En `src/app/App.tsx`, junto a los demas handlers del explorer (tras `handleEnterFolder`, linea ~548):

```ts
  const handleFocusOnExplorer = useCallback(
    (file: string) => {
      void setRightPanelOpen(true);
      void setRightPanelActiveTab("explorer");
      if (activeWorkspace) {
        const target = resolveFocusTarget({
          file,
          mode: activeRootMode,
          currentRoot: explorerRoot,
          fsRoot: fsFolderRoot,
          home,
        });
        if (target) {
          setExplorerRootMode(activeWorkspace.id, target.nextMode);
          setFsRoot(activeWorkspace.id, target.nextFsRoot);
        }
      }
      setRevealRequest((r) => ({ path: file, nonce: (r?.nonce ?? 0) + 1 }));
    },
    [
      activeWorkspace,
      activeRootMode,
      explorerRoot,
      fsFolderRoot,
      home,
      setExplorerRootMode,
      setFsRoot,
    ],
  );
```

Nota: `setRightPanelActiveTab` se importa de `@/modules/settings/store`; verificar que ya esta importado en `App.tsx` (lo usa `navigateRightPanelTo`). Si no lo estuviera, anadirlo al import existente de ese modulo.

- [ ] **Step 5: Registrar el callback en panelCallbacks**

En `src/app/App.tsx`, dentro del objeto `panelCallbacks` (tras `onRenameFile: ...`, linea ~1120):

```ts
      onFocusOnExplorer: (filePath) => handleFocusOnExplorer(filePath),
```

Anadir `handleFocusOnExplorer` al array de dependencias del `useMemo` de `panelCallbacks` (linea ~1122-1134).

- [ ] **Step 6: Pasar revealRequest a las dos instancias de RightPanel**

En `src/app/App.tsx`, en CADA una de las dos `<RightPanel ...>` (lineas ~1938 y ~2017), tras `activeFilePath={explorerActiveFilePath ?? null}`:

```tsx
                        revealRequest={revealRequest}
```

- [ ] **Step 7: Verificar tipos, lint y tests**

Run: `pnpm check-types && pnpm lint && pnpm test`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add src/modules/workspaces/PanelContent.tsx src/app/App.tsx
git commit -m "feat(explorer): wire focus-on-explorer handler in App"
```

---

## Task 5: Item de menu en PaneTabBar

**Files:**
- Modify: `src/modules/workspaces/PaneTabBar.tsx`
- Modify: `src/modules/workspaces/PaneView.tsx`

**Interfaces:**
- Consumes: `panelFilePath` de `./lib/panelPath`; `CrosshairIcon` de `@hugeicons/core-free-icons`; `HugeiconsIcon` (ya importado); `callbacks.onFocusOnExplorer` (de Task 4).
- Produces: item de menu "Focus on Explorer" visible cuando `panelFilePath(panel)` no es null; prop `onFocusOnExplorer?: (filePath: string) => void` en `PaneTabBar` y `DraggableTab`.

- [ ] **Step 1: Imports en PaneTabBar**

En `src/modules/workspaces/PaneTabBar.tsx`:

Anadir `CrosshairIcon` al import de hugeicons (linea 27):

```ts
import { Alert02Icon, Copy01Icon, CrosshairIcon, LinkSquare02Icon, LockKeyIcon, PencilEdit01Icon, Tick02Icon } from "@hugeicons/core-free-icons";
```

Anadir el import del helper (junto a los demas imports `./...` del fichero):

```ts
import { panelFilePath } from "./lib/panelPath";
```

- [ ] **Step 2: Anadir la prop al type Props y a DraggableTab**

En `type Props` (linea ~492, tras `onRenamePanel?: ...`):

```ts
  onFocusOnExplorer?: (filePath: string) => void;
```

En la firma inline de `DraggableTab` (el bloque de tipos `}: { ... }`, tras `onRenamePanel?: ...`, linea ~550):

```ts
  onFocusOnExplorer?: (filePath: string) => void;
```

En la desestructuracion de `DraggableTab` (tras `onRenamePanel,`, linea ~520):

```ts
  onFocusOnExplorer,
```

- [ ] **Step 3: Anadir el item de menu**

En `src/modules/workspaces/PaneTabBar.tsx`, dentro de `DraggableTab`, calcular el path cerca de `const isLockable = ...` (linea ~572):

```ts
  const focusFilePath = panelFilePath(panel);
```

Anadir el item como primer bloque dentro de `<ContextMenuContent onCloseAutoFocus={(e) => e.preventDefault()}>` (justo despues de la linea 924, antes del bloque `{onRenamePanel && (`):

```tsx
            {onFocusOnExplorer && focusFilePath && (
              <>
                <ContextMenuItem onSelect={() => onFocusOnExplorer(focusFilePath)}>
                  <HugeiconsIcon icon={CrosshairIcon} size={14} strokeWidth={2} />
                  Focus on Explorer
                </ContextMenuItem>
                <ContextMenuSeparator />
              </>
            )}
```

- [ ] **Step 4: Pasar la prop de PaneTabBar a DraggableTab**

En la firma de `export function PaneTabBar({ ... }: Props)` (linea 1053), anadir `onFocusOnExplorer` a la lista desestructurada.

En el JSX `<DraggableTab ... />` (linea ~1234), tras `onRenamePanel={onRenamePanel}` (linea ~1256):

```tsx
          onFocusOnExplorer={onFocusOnExplorer}
```

- [ ] **Step 5: Pasar el callback desde PaneView**

En `src/modules/workspaces/PaneView.tsx`, en `<PaneTabBar ...>` (linea ~246), tras `onRenamePanel={callbacks.onRenamePanel}` (linea ~263):

```tsx
        onFocusOnExplorer={callbacks.onFocusOnExplorer}
```

- [ ] **Step 6: Verificar tipos, lint y tests**

Run: `pnpm check-types && pnpm lint && pnpm test`
Expected: PASS.

- [ ] **Step 7: Verificacion manual**

Run: `pnpm tauri dev` (proceso fresco, no confiar en HMR para estado mutable).

Comprobar:
1. Abrir un fichero profundo en el editor. Cambiar el Explorer a "Follow Git" / "Follow Terminal" / "Workspace Root".
2. Click derecho en la tab del fichero -> "Focus on Explorer" (con icono de mirilla).
   - Si la vista ya contiene el fichero: se revela sin cambiar de modo.
   - Si no: salta a "File System" con el root correcto (ancestro comun) y revela.
3. El foco permanece en el editor (no salta al explorer).
4. Funciona sobre una tab que NO es la activa.
5. Probar tambien sobre una tab markdown, una git-diff y una git-commit-file.
6. Verificar que la opcion NO aparece en tabs de terminal ni browser.

- [ ] **Step 8: Commit**

```bash
git add src/modules/workspaces/PaneTabBar.tsx src/modules/workspaces/PaneView.tsx
git commit -m "feat(workspaces): add Focus on Explorer to tab context menu"
```

---

## Self-Review

**Spec coverage:**
- Item solo en tabs con fichero (editor/markdown/git-diff/git-commit-file) -> Task 5 (`panelFilePath` + condicion `focusFilePath`), Task 2.
- Icono mirilla `CrosshairIcon` -> Task 5 Step 1/3.
- No roba foco -> Task 3 Step 2 (sin `focus()`), Task 5 Step 7 verificacion 3.
- Vista actual ya contiene el fichero -> revelar sin cambiar -> Task 1 (`resolveFocusTarget` devuelve null) + Task 3 driver.
- Vista no contiene -> filesystem + ancestro comun, fallback dirname -> Task 1 (`resolveFocusTarget`/`commonAncestor`).
- Abrir right panel + tab explorer -> Task 4 Step 4.
- Coordinacion async (cambio root -> carga -> expandir -> seleccionar -> scroll) -> Task 3 driver.
- Tests de invariantes -> Task 1, Task 2.

**Placeholder scan:** Sin TBD/TODO; todo el codigo va explicito.

**Type consistency:** `RevealRequest = { path: string; nonce: number }` definido en Task 3, consumido igual en RightPanel (Task 3) y App (Task 4). `onFocusOnExplorer: (filePath: string) => void` identico en PanelCallbacks (Task 4), PaneTabBar Props/DraggableTab (Task 5) y PaneView (Task 5). `resolveFocusTarget` firma identica entre Task 1 (definicion) y Task 4 (uso). `panelFilePath(panel)` identico entre Task 2 y Task 5.
